# 🖥️ Требования к серверу для деплоя EAIP

## 📋 Минимальные системные требования

### Обязательные требования:
- **ОС:** Linux (Ubuntu 20.04+ / Debian 11+ / CentOS 8+)
- **RAM:** Минимум 4GB (рекомендуется 8GB+)
- **CPU:** 2+ ядра (рекомендуется 4+)
- **Диск:** 20GB+ свободного места (рекомендуется 50GB+)
- **Сеть:** Стабильное интернет-соединение
- **Доступ:** SSH доступ с правами sudo

### Рекомендуемые требования:
- **RAM:** 8GB+ (для комфортной работы с ИИ и OCR)
- **CPU:** 4+ ядра (для параллельной обработки файлов)
- **Диск:** 50GB+ SSD (для быстрой работы БД и файлов)
- **Сеть:** 100 Mbps+ (для загрузки Docker образов)

## 🛠️ Необходимое ПО на сервере

### 1. Docker и Docker Compose (обязательно)

**Установка Docker:**
```bash
# Ubuntu/Debian
curl -fsSL https://get.docker.com | sh
sudo usermod -aG docker $USER

# Проверка
docker --version
docker compose version
```

**Требования:**
- Docker Engine 20.10+
- Docker Compose 2.0+

### 2. Git (для клонирования репозитория)

```bash
# Ubuntu/Debian
sudo apt-get update
sudo apt-get install -y git

# Проверка
git --version
```

### 3. Базовые утилиты

```bash
# Ubuntu/Debian
sudo apt-get install -y curl wget unzip tar
```

## 📦 Что нужно скопировать на сервер

### Вариант 1: Клонирование репозитория (рекомендуется)

```bash
# На сервере
cd /opt
sudo git clone https://github.com/Sinergys/eaip-full-skeleton-pdf.git eaip
sudo chown -R $USER:$USER /opt/eaip
cd /opt/eaip
```

### Вариант 2: Копирование только нужных файлов

**Создать архив на локальной машине:**
```bash
# На локальной машине
cd eaip_full_skeleton
tar -czf eaip-deploy.tar.gz \
  infra/docker-compose.staging.yml \
  infra/deploy-staging.sh \
  infra/harden-staging.sh \
  infra/post-deploy-checks.sh \
  infra/rollback-staging.sh \
  infra/update-version.sh \
  infra/QUICK_STAGING.md \
  infra/STAGING_DEPLOYMENT.md \
  infra/setup-*.sh \
  DEPLOYMENT_CHECKLIST.md \
  SERVER_REQUIREMENTS.md
```

**Скопировать на сервер:**
```bash
# С локальной машины
scp eaip-deploy.tar.gz user@server:/tmp/

# На сервере
cd /opt
sudo mkdir -p eaip/infra
sudo tar -xzf /tmp/eaip-deploy.tar.gz -C eaip
sudo chown -R $USER:$USER /opt/eaip
cd /opt/eaip/infra
```

## 🔧 Настройка сервера

### 1. Создать директорию для проекта

```bash
sudo mkdir -p /opt/eaip
sudo chown -R $USER:$USER /opt/eaip
cd /opt/eaip
```

### 2. Настроить firewall (опционально, но рекомендуется)

```bash
# Ubuntu/Debian (ufw)
sudo ufw allow 22/tcp    # SSH
sudo ufw allow 80/tcp    # HTTP (gateway-auth)
sudo ufw allow 443/tcp   # HTTPS (если используется)
sudo ufw enable

# Проверка
sudo ufw status
```

### 3. Настроить swap (если RAM < 4GB)

```bash
# Создать swap файл 2GB
sudo fallocate -l 2G /swapfile
sudo chmod 600 /swapfile
sudo mkswap /swapfile
sudo swapon /swapfile

# Сделать постоянным
echo '/swapfile none swap sw 0 0' | sudo tee -a /etc/fstab
```

### 4. Настроить переменные окружения

**Создать файл `/opt/eaip/infra/.env`:**

```bash
cd /opt/eaip/infra
cat > .env <<EOF
# PostgreSQL Configuration
POSTGRES_USER=eaip_user
POSTGRES_PASSWORD=$(openssl rand -base64 32)
POSTGRES_DB=eaip_db
POSTGRES_HOST=postgres

# MinIO Configuration
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=$(openssl rand -base64 32)

# AI Configuration (опционально)
AI_PROVIDER=deepseek
AI_ENABLED=true
AI_PREFER_FOR_PDF=true
DEEPSEEK_API_KEY=sk-fa4d5adfd79d4307809a34b153fc0ab7
DEEPSEEK_MODEL=deepseek-chat
EOF

# Защитить файл
chmod 600 .env
```

## 🚀 Быстрый деплой

### Шаг 1: Подготовка

```bash
cd /opt/eaip/infra

# Сделать скрипты исполняемыми
chmod +x *.sh

# Проверить наличие docker-compose.staging.yml
ls -la docker-compose.staging.yml
```

### Шаг 2: Запуск деплоя

```bash
# Автоматический деплой
bash deploy-staging.sh

# Или вручную
docker compose -f docker-compose.staging.yml pull
docker compose -f docker-compose.staging.yml up -d
```

### Шаг 3: Проверка

```bash
# Статус контейнеров
docker compose -f docker-compose.staging.yml ps

# Health check
curl http://localhost/health

# Логи
docker compose -f docker-compose.staging.yml logs -f
```

## 📊 Проверка готовности сервера

### Чеклист перед деплоем:

- [ ] Docker установлен и работает
- [ ] Docker Compose установлен
- [ ] Git установлен (если клонируем репозиторий)
- [ ] Достаточно места на диске (`df -h`)
- [ ] Достаточно RAM (`free -h`)
- [ ] Порты 80, 8001-8006 свободны (`netstat -tuln | grep -E '80|800[1-6]'`)
- [ ] Firewall настроен (если используется)
- [ ] SSH доступ работает
- [ ] Пользователь в группе docker (`groups | grep docker`)
- [ ] Файлы проекта скопированы в `/opt/eaip`
- [ ] `.env` файл создан с правильными значениями
- [ ] Скрипты исполняемые (`chmod +x *.sh`)

### Команда для быстрой проверки:

```bash
#!/bin/bash
echo "Проверка готовности сервера..."
echo "Docker: $(docker --version 2>&1 || echo 'НЕ УСТАНОВЛЕН')"
echo "Docker Compose: $(docker compose version 2>&1 || echo 'НЕ УСТАНОВЛЕН')"
echo "Git: $(git --version 2>&1 || echo 'НЕ УСТАНОВЛЕН')"
echo "RAM: $(free -h | grep Mem | awk '{print $2}')"
echo "Диск: $(df -h / | tail -1 | awk '{print $4}') свободно"
echo "Порты:"
netstat -tuln 2>/dev/null | grep -E ':(80|800[1-6])' || echo "  Порты свободны"
echo "Docker группа: $(groups | grep -q docker && echo 'OK' || echo 'НЕТ')"
```

## 🔐 Безопасность

### Обязательные меры:

1. **Изменить дефолтные пароли:**
   - PostgreSQL пароль в `.env`
   - MinIO пароль в `.env`

2. **Настроить firewall:**
   ```bash
   sudo ufw allow 22/tcp  # SSH
   sudo ufw allow 80/tcp  # HTTP
   sudo ufw enable
   ```

3. **Защитить .env файл:**
   ```bash
   chmod 600 /opt/eaip/infra/.env
   ```

4. **Настроить SSL/TLS** (для production):
   - Использовать reverse proxy (Nginx, Caddy)
   - Настроить Let's Encrypt сертификаты

5. **Регулярные бэкапы:**
   ```bash
   # Настроить автоматические бэкапы БД
   bash setup-backups.sh
   ```

## 🌐 Сетевые требования

### Порты, которые будут использоваться:

- **80** - gateway-auth (основной HTTP)
- **443** - HTTPS (если настроен)
- **8001-8006** - внутренние сервисы (можно закрыть для внешнего доступа)
- **9000** - MinIO API (внутренний)
- **9001** - MinIO Console (можно закрыть)
- **5432** - PostgreSQL (только внутри Docker сети)
- **6379** - Redis (только внутри Docker сети)

### Рекомендации:

- Открыть только порт 80 (или 443 для HTTPS) для внешнего доступа
- Остальные порты использовать только внутри Docker сети
- Использовать reverse proxy для маршрутизации

## 💾 Хранение данных

### Volumes Docker:

- **PostgreSQL данные:** `/var/lib/docker/volumes/infra_pgdata`
- **MinIO данные:** `/var/lib/docker/volumes/infra_minio`
- **Загруженные файлы:** `/opt/eaip/infra/data/inbox`

### Рекомендации:

- Настроить регулярные бэкапы volumes
- Мониторить использование дискового пространства
- Использовать SSD для БД (лучшая производительность)

## 📈 Мониторинг ресурсов

### Проверка использования:

```bash
# Использование CPU и RAM
docker stats

# Использование диска
df -h
docker system df

# Логи контейнеров
docker compose -f docker-compose.staging.yml logs --tail=100
```

## 🆘 Решение проблем

### Проблема: Недостаточно места на диске

**Решение:**
```bash
# Очистить неиспользуемые Docker данные
docker system prune -a

# Проверить большие файлы
du -sh /var/lib/docker/volumes/*
```

### Проблема: Контейнеры не запускаются

**Решение:**
```bash
# Проверить логи
docker compose -f docker-compose.staging.yml logs

# Проверить статус
docker compose -f docker-compose.staging.yml ps

# Перезапустить
docker compose -f docker-compose.staging.yml restart
```

### Проблема: Порты заняты

**Решение:**
```bash
# Найти процесс, использующий порт
sudo lsof -i :80
sudo lsof -i :8001

# Остановить процесс или изменить порты в docker-compose.staging.yml
```

## 📚 Дополнительная документация

- `DEPLOYMENT_CHECKLIST.md` - полный чеклист деплоя
- `infra/STAGING_DEPLOYMENT.md` - подробное руководство
- `infra/QUICK_STAGING.md` - быстрая инструкция
- `AI_INTEGRATION_PLAN.md` - настройка ИИ

## ✅ Итоговый чеклист

Перед началом деплоя убедитесь:

- [ ] Сервер соответствует минимальным требованиям
- [ ] Docker и Docker Compose установлены
- [ ] Git установлен (если клонируем)
- [ ] Файлы проекта скопированы
- [ ] `.env` файл создан с правильными значениями
- [ ] Firewall настроен
- [ ] Порты свободны
- [ ] Достаточно ресурсов (RAM, диск)
- [ ] Пользователь в группе docker
- [ ] Скрипты исполняемые

**Готово к деплою!** 🚀

