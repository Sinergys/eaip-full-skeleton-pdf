# 🤖 Промпт для ИИ: Проблема с веб-интерфейсом загрузки файлов

## Краткий промпт (скопируй и вставь):

```
Проблема: Веб-интерфейс для загрузки файлов не работает в EAIP системе.

Контекст:
- FastAPI микросервис на порту 8001
- Endpoint: POST /ingest/files (multipart/form-data)
- Python HTTP сервер на порту 8080 для статики
- HTML файл: client/index.html
- Docker контейнер: eaip-ingest-local

Проблемы:
1. HTML интерфейс не отображается в браузере (http://localhost:8080/)
2. Swagger UI не показывает кнопку "Choose File" для endpoint с файлами
3. API работает (проверено через curl), но веб-интерфейс недоступен

Что нужно:
- Рабочий веб-интерфейс для загрузки файлов через браузер
- Форма с выбором файла и кнопкой загрузки
- Отправка multipart/form-data на http://localhost:8001/ingest/files
- Отображение результата (Batch ID, статус валидации)

Технические детали:
- ОС: Windows 10
- Docker контейнер работает
- HTTP сервер запущен на порту 8080
- CORS может быть проблемой

Создай простое решение, которое точно работает.
```

## Детальный промпт (для глубокого анализа):

См. файл `ISSUE_WEB_UPLOAD_INTERFACE.md` для полного описания проблемы со всеми техническими деталями.

