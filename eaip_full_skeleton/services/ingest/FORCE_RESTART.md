# 🔄 ПРИНУДИТЕЛЬНЫЙ ПЕРЕЗАПУСК СЕРВЕРА

## ⚠️ Проблема: Сервер не видит новые endpoints

Сервер запущен с `--reload`, но не подхватывает изменения. Нужен **ручной перезапуск**.

## 🚀 Решение: Полная остановка и перезапуск

### Шаг 1: Остановите сервер

В терминале, где запущен uvicorn:
1. Нажмите **Ctrl+C** (дважды, если нужно)
2. Дождитесь сообщения "Shutting down"

### Шаг 2: Убедитесь, что процесс остановлен

```powershell
# Проверьте процессы
Get-Process python -ErrorAction SilentlyContinue | Where-Object {$_.Id -eq 15472}

# Если процесс все еще работает, убейте его:
Stop-Process -Id 15472 -Force
```

### Шаг 3: Очистите кеш (опционально, но рекомендуется)

```powershell
cd C:\eaip\eaip_full_skeleton\services\ingest
Get-ChildItem -Recurse -Filter "__pycache__" | Remove-Item -Recurse -Force
```

### Шаг 4: Запустите сервер заново

```powershell
cd C:\eaip\eaip_full_skeleton\services\ingest
uvicorn main:app --reload --port 8001
```

### Шаг 5: Проверьте endpoints

После запуска проверьте:

```powershell
# Простой тест
curl http://localhost:8001/test-xlsm

# Должно вернуть:
# {"xlsm_in_allowed":true,"allowed_extensions":[...],"message":"Тест поддержки .xlsm"}

# Debug endpoint
curl http://localhost:8001/api/debug/extensions
```

## ✅ После перезапуска

1. **Endpoint `/test-xlsm` должен работать**
2. **Endpoint `/api/debug/extensions` должен работать**
3. **Загрузка `.xlsm` файлов должна работать**

## 🔍 Если endpoints все еще не работают

Проверьте логи сервера при запуске - должны быть сообщения:
```
INFO:     Application startup complete.
```

Если видите ошибки импорта или другие проблемы - пришлите логи.

