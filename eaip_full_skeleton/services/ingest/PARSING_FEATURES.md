# Функциональность распознавания и извлечения данных

## Дата реализации: 2025-01-XX

## Реализованные возможности согласно ТЗ

### ✅ Автоматическое распознавание после загрузки

После успешной загрузки файла система автоматически:
1. **Распознает тип файла** (PDF, Excel, Word, изображения)
2. **Извлекает данные** из файла
3. **Формирует отчет** о распознанных данных
4. **Сохраняет результаты** для последующего доступа

### ✅ Поддерживаемые форматы

#### PDF файлы
- ✅ Извлечение текста со всех страниц
- ✅ Извлечение таблиц (через pdfplumber)
- ✅ Метаданные документа
- ✅ Статистика: количество страниц, символов, таблиц

#### Excel файлы (XLSX)
- ✅ Чтение всех листов
- ✅ Извлечение данных из ячеек
- ✅ Статистика: количество листов, строк, столбцов

#### Word файлы (DOCX)
- ✅ Извлечение параграфов
- ✅ Извлечение таблиц
- ✅ Статистика: количество параграфов и таблиц

### ✅ API Endpoints

#### `POST /web/upload`
Загрузка файла через веб-интерфейс с автоматическим распознаванием.

**Ответ включает:**
- `batch_id` - уникальный идентификатор загрузки
- `parsing_status` - статус распознавания (success/error/pending)
- `parsing_summary` - краткая сводка результатов

#### `GET /ingest/parse/{batch_id}`
Получение полных результатов распознавания файла.

**Пример ответа:**
```json
{
  "batch_id": "9bcff6c2-4c70-46b7-b50b-f43f42cec8ad",
  "filename": "document.pdf",
  "file_type": "PDF",
  "parsing": {
    "file_type": "pdf",
    "parsed": true,
    "data": {
      "pages": [...],
      "text": "извлеченный текст...",
      "tables": [...],
      "metadata": {...}
    }
  },
  "status": "success"
}
```

#### `GET /ingest/parse/{batch_id}/summary`
Получение краткой сводки результатов распознавания.

**Пример ответа для PDF:**
```json
{
  "batch_id": "9bcff6c2-4c70-46b7-b50b-f43f42cec8ad",
  "filename": "document.pdf",
  "file_type": "pdf",
  "status": "success",
  "parsed": true,
  "pages": 5,
  "total_characters": 12345,
  "total_tables": 2,
  "has_text": true,
  "has_tables": true
}
```

### ✅ Веб-интерфейс

После загрузки файла веб-интерфейс автоматически отображает:
- ✅ Статус загрузки
- ✅ Batch ID
- ✅ Информацию о файле (тип, размер)
- ✅ **Результаты распознавания** (страницы, символы, таблицы и т.д.)
- ✅ Ссылку на подробный отчет

### 📊 Пример работы с PDF файлом

1. **Загрузка:** Пользователь загружает `TTZ 133AA faktura.pdf`
2. **Распознавание:** Система автоматически извлекает:
   - Текст со всех страниц
   - Таблицы (если есть)
   - Метаданные документа
3. **Отчет:** Пользователь видит:
   - Количество страниц
   - Количество символов
   - Количество найденных таблиц
   - Ссылку на подробный отчет

### 🔧 Технические детали

#### Библиотеки для парсинга:
- `pdfplumber==0.10.3` - основной парсер PDF (поддержка таблиц)
- `PyPDF2==3.0.1` - резервный парсер PDF
- `openpyxl==3.1.2` - парсинг Excel
- `python-docx==1.1.0` - парсинг Word

#### Хранение результатов:
- Временное хранение в памяти (словарь `parsing_results`)
- В продакшене рекомендуется использовать Redis или PostgreSQL

#### Обработка ошибок:
- Ошибки парсинга логируются
- Пользователю показывается понятное сообщение об ошибке
- Статус `error` сохраняется в результатах

### 🚀 Следующие шаги

1. **Интеграция с Redis/PostgreSQL** для постоянного хранения результатов
2. **OCR для изображений** (JPG, PNG) через pytesseract
3. **Структурирование данных** из эталонного шаблона `EnergyPassport_v1.1.2.xlsx`
4. **Асинхронная обработка** больших файлов через Celery
5. **Экспорт результатов** в различные форматы (JSON, CSV)

---

**Статус:** ✅ Базовая функциональность распознавания реализована и работает

