# 🤖 Настройка ИИ для распознавания документов

## Быстрый старт с DeepSeek

### 1. Получить API ключ

1. Зарегистрируйтесь на https://platform.deepseek.com
2. Получите API ключ в разделе API Keys
3. Скопируйте ключ (начинается с `sk-...`)

### 2. Настроить переменные окружения

**Для локальной разработки:**

Создайте файл `infra/.env` или добавьте в существующий:

```env
# AI Configuration
AI_PROVIDER=deepseek
AI_ENABLED=true
AI_PREFER_FOR_PDF=true  # Использовать ИИ для PDF вместо OCR
DEEPSEEK_API_KEY=sk-ваш-ключ-здесь
DEEPSEEK_MODEL=deepseek-chat
```

**Для staging/production:**

Добавьте в `infra/.env` на сервере:

```env
AI_PROVIDER=deepseek
AI_ENABLED=true
AI_PREFER_FOR_PDF=true
DEEPSEEK_API_KEY=sk-...
DEEPSEEK_MODEL=deepseek-chat
```

### 3. Пересобрать и перезапустить сервис

```bash
# Локально
cd infra
docker compose build ingest
docker compose up -d ingest

# На сервере (staging)
cd /opt/eaip/infra
docker compose -f docker-compose.staging.yml build ingest
docker compose -f docker-compose.staging.yml up -d ingest
```

### 4. Проверить работу

```bash
# Проверить логи
docker compose logs ingest | grep -i "ai\|deepseek"

# Должно быть:
# INFO: DeepSeek API настроен, модель: deepseek-chat
```

## Использование

### Автоматическое распознавание

После настройки ИИ будет автоматически использоваться для:
- PDF файлов (если `AI_PREFER_FOR_PDF=true`)
- Изображений (JPG, PNG)

### Переменные окружения

| Переменная | Описание | Значение по умолчанию |
|-----------|----------|----------------------|
| `AI_PROVIDER` | Провайдер ИИ | `deepseek` |
| `AI_ENABLED` | Включить ИИ | `false` |
| `AI_PREFER_FOR_PDF` | Использовать ИИ для PDF вместо OCR | `false` |
| `DEEPSEEK_API_KEY` | API ключ DeepSeek | - |
| `DEEPSEEK_MODEL` | Модель DeepSeek | `deepseek-chat` |

## Альтернативные провайдеры

### OpenAI

```env
AI_PROVIDER=openai
AI_ENABLED=true
OPENAI_API_KEY=sk-...
OPENAI_MODEL_VISION=gpt-4-vision-preview
OPENAI_MODEL_TEXT=gpt-4
```

### Anthropic Claude

```env
AI_PROVIDER=anthropic
AI_ENABLED=true
ANTHROPIC_API_KEY=sk-ant-...
ANTHROPIC_MODEL=claude-3-opus-20240229
```

## Режимы работы

### Режим 1: Только традиционный парсинг (по умолчанию)
```env
AI_ENABLED=false
```
- Используется pdfplumber/PyPDF2 для PDF
- Tesseract OCR для сканированных документов
- Без использования ИИ

### Режим 2: Гибридный (ИИ как fallback)
```env
AI_ENABLED=true
AI_PREFER_FOR_PDF=false
```
- Сначала традиционный парсинг
- Если мало текста → OCR
- ИИ не используется автоматически

### Режим 3: ИИ для PDF (рекомендуется)
```env
AI_ENABLED=true
AI_PREFER_FOR_PDF=true
```
- PDF обрабатываются через ИИ Vision API
- Лучшее качество распознавания
- Поддержка сложных документов

### Режим 4: ИИ для изображений
```env
AI_ENABLED=true
```
- Изображения (JPG, PNG) обрабатываются через ИИ
- Автоматически для всех изображений

## Проверка работы

### Тест через веб-интерфейс

1. Откройте http://localhost:8001/web/upload
2. Загрузите PDF или изображение
3. Проверьте результаты:
   - Должно быть `"ai_used": true` в ответе
   - Должен быть извлечен текст

### Тест через API

```bash
curl -X POST http://localhost:8001/web/upload \
  -F "file=@test.pdf" | jq '.parsing.data.ai_used'
# Должно вернуть: true
```

### Проверка логов

```bash
docker compose logs ingest | grep -i "ии\|ai\|deepseek"
```

Ожидаемые сообщения:
```
INFO: DeepSeek API настроен, модель: deepseek-chat
INFO: Используется ИИ для распознавания PDF
INFO: Начинаю ИИ распознавание файла: ...
INFO: Обрабатываю страницу 1/5 через ИИ...
INFO: ИИ распознавание завершено: извлечено 1234 символов из 5 страниц
```

## Устранение проблем

### Ошибка: "DEEPSEEK_API_KEY не установлен"
**Решение:** Проверьте, что переменная установлена в `.env` файле

### Ошибка: "openai библиотека не установлена"
**Решение:** 
```bash
docker compose build ingest
```

### ИИ не используется
**Проверьте:**
1. `AI_ENABLED=true` в .env
2. `AI_PREFER_FOR_PDF=true` для PDF (если нужно)
3. API ключ валидный
4. Логи на наличие ошибок

### Высокая стоимость
**Решение:**
- Используйте DeepSeek (дешевле в 200+ раз)
- Включите кэширование результатов
- Ограничьте использование ИИ только для сложных документов

## Стоимость (примерная)

### DeepSeek
- ~$0.00014 за 1K токенов
- 10 страниц PDF ≈ $0.01-0.05

### OpenAI GPT-4 Vision
- ~$0.01-0.03 за изображение
- 10 страниц PDF ≈ $0.30-0.50

## Безопасность

⚠️ **Важно:**
- Не коммитьте `.env` файл в git
- Храните API ключи в секретах (GitHub Secrets, Docker Secrets)
- Используйте разные ключи для staging и production
- Мониторьте использование API

## Дополнительная информация

- Подробный план интеграции: `AI_INTEGRATION_PLAN.md`
- Документация DeepSeek: https://platform.deepseek.com/docs

