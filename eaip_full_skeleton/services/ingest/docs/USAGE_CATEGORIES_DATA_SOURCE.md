# Источник данных по категориям использования электроэнергии (by_usage)

**Дата:** 2025-01-XX  
**Модуль:** `utils/energy_aggregator.py`

---

## 📋 ОБЗОР

Данные по категориям использования электроэнергии (`electricity.by_usage`) извлекаются из специального файла Excel с разбивкой по категориям потребления.

---

## 📁 ИСТОЧНИКИ ДАННЫХ

### ⭐ Основной источник: `oborudovanie.xlsx`

**Расположение:** `data/source_files/audit_sinergys/oborudovanie.xlsx`

**Роль:** Основной источник данных по категориям использования электроэнергии

**Структура:**
- Лист1 содержит иерархическую структуру оборудования по цехам/помещениям
- Названия разделов в объединенных ячейках используются для определения категории использования
- ИИ классифицирует оборудование по категориям: `technological`, `own_needs`, `production`, `household`

**Документация:** См. `docs/EQUIPMENT_FILE_STRUCTURE_ANALYSIS.md`

---

### 📊 Вспомогательный источник: `pererashod.xlsx`

**Расположение:** `data/source_files/audit_sinergys/pererashod.xlsx`

**Роль:** Вспомогательный источник для сверки расхождений и возможности корректировки

**Структура файла:**
- **4 таблицы**, разделенные 2 пустыми строками
- Каждая таблица начинается с R1C1 (A1)
- **4-я таблица** содержит данные по категориям использования (строки 26-37)

**Структура 4-й таблицы:**
- **Строка 26:** Заголовки - "Год", 2022, 2023, 2024, ИТОГО (столбцы B-F)
- **Строка 29:** "Тех-потери кВтч" → категория `technological` (столбцы C-E для годов)
- **Строка 30:** "Хоз-бытовые нужды кВтч" → категория `household`
- **Строка 31:** "Производственные нужды" → категория `production`
- **Категория `own_needs`:** Устанавливается в 0 (не присутствует в файле, рассчитывается из oborudovanie.xlsx)

---

## 🔧 ФУНКЦИЯ ПАРСИНГА

### `aggregate_usage_categories(workbook_path)`

**Файл:** `eaip_full_skeleton/services/ingest/utils/energy_aggregator.py`  
**Строки:** 778-832

**Что делает:**
1. Проверяет, что файл содержит "pererashod" в названии
2. Читает годы из строки 1, столбцы 16-18
3. Читает значения категорий из строк 4-6
4. Формирует структуру:
   ```python
   {
       "source": "path/to/pererashod.xlsx",
       "generated_at": "2025-01-XX...",
       "years": {
           "2022": {
               "technological": 12345.0,
               "household": 6789.0,
               "production": 2345.0,
               "own_needs": 0.0
           },
           "2023": {...},
           "2024": {...}
       }
   }
   ```

---

## 📊 РАСПРЕДЕЛЕНИЕ ПО КВАРТАЛАМ

### `distribute_categories_by_quarter(aggregated_data, categories_data)`

**Файл:** `eaip_full_skeleton/services/ingest/utils/energy_aggregator.py`  
**Строки:** 835-874

**Что делает:**
1. Берет годовые данные по категориям из `aggregate_usage_categories()`
2. Распределяет их пропорционально по кварталам на основе `quarter_totals.active_kwh`
3. Добавляет поле `by_usage` в каждый квартал:
   ```python
   {
       "2022-Q1": {
           "quarter_totals": {"active_kwh": 30000, ...},
           "by_usage": {
               "technological": 10000.0,  # пропорционально
               "household": 5000.0,
               "production": 2000.0,
               "own_needs": 0.0
           }
       },
       ...
   }
   ```

**Формула распределения:**
```python
quarter_by_usage[category] = (quarter_total * yearly_category_value / yearly_total)
```

---

## 🔗 ИСПОЛЬЗОВАНИЕ В КОДЕ

### В `main.py` (строка 1262):

```python
usage_categories = aggregate_usage_categories(dst)
if usage_categories:
    aggregated_data = distribute_categories_by_quarter(
        aggregated_data, 
        usage_categories
    )
```

**Процесс:**
1. При загрузке файлов электроэнергии ищется файл `pererashod.xlsx`
2. Если найден - парсятся категории использования
3. Годовые категории распределяются по кварталам пропорционально потреблению
4. Результат добавляется в `resources.electricity.{quarter}.by_usage`

---

## ⚠️ ВАЖНЫЕ ЗАМЕЧАНИЯ

### 1. Приоритет источников данных

**Основной источник:** `oborudovanie.xlsx`
- Более точные данные по категориям использования
- Классификация через ИИ на основе названий разделов и оборудования
- Используется для первичного определения категорий

**Вспомогательный источник:** `pererashod.xlsx`
- Используется для **сверки** и **корректировки** данных
- Помогает выявить расхождения
- Позволяет скорректировать категории при необходимости

### 2. Зависимость от файлов

- **Если `oborudovanie.xlsx` отсутствует** → используются только данные из `pererashod.xlsx`
- **Если `pererashod.xlsx` отсутствует** → используются только данные из `oborudovanie.xlsx`
- **Если оба файла отсутствуют** → `by_usage` не будет заполнен

### 3. Использование ИИ для анализа

- **Для `pererashod.xlsx`:** ИИ анализирует структуру 4-й таблицы для определения категорий
- **Для `oborudovanie.xlsx`:** ИИ классифицирует оборудование по категориям использования на основе названий разделов

### 4. Категория `own_needs`

- В файле `pererashod.xlsx` **не присутствует**
- Рассчитывается из `oborudovanie.xlsx` на основе классификации оборудования
- Если данные оборудования отсутствуют, устанавливается в `0.0`

---

## 📝 MAPPING КАТЕГОРИЙ

| Строка в файле | Название в файле | Код категории | Описание |
|----------------|------------------|---------------|----------|
| 4 | "Тех-потери кВтч" | `technological` | Технологическое потребление |
| 5 | "Хоз-бытовые нужды кВтч" | `household` | Хоз-бытовое потребление |
| 6 | "Производственные нужды" | `production` | Производственное потребление |
| - | (отсутствует) | `own_needs` | Собственные нужды (устанавливается в 0) |

---

## 🔍 ПРОВЕРКА ДАННЫХ

### Валидация в `domain/passport_requirements.py`:

Лист `'04_Баланс'` требует обязательное поле:
- `electricity.by_usage` - с категориями для каждого квартала

**Проверка:**
- Минимум 4 квартала
- Обязательные категории: `technological`, `own_needs`, `production`, `household`

---

## 🔗 СВЯЗАННЫЕ МОДУЛИ

- `utils/energy_aggregator.py` - Парсинг и агрегация данных
- `domain/passport_requirements.py` - Требования к данным
- `utils/canonical_to_passport.py` - Альтернативный расчет из оборудования
- `domain/electricity_usage_classifier.py` - Классификация оборудования

---

## Конец документации

