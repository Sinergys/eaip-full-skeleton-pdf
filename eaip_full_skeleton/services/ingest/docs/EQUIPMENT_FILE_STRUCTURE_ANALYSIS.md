# Анализ структуры файла оборудования `oborudovanie.xlsx`

**Дата анализа:** 2025-01-XX  
**Файл:** `data/source_files/audit_sinergys/oborudovanie.xlsx`

---

## 📋 ОБЗОР

Файл содержит данные об оборудовании предприятия, организованные по цехам и помещениям. 

**Важно:** 
- **Лист1** - используется для парсинга (иерархическая структура с объединенными ячейками)
- **Лист2 и Лист3** - игнорируются (сплошной список оборудования без структуры)

---

## 🏗️ СТРУКТУРА ФАЙЛА

### Лист1 (139 строк, 9 столбцов)

**Структура:** Иерархическая с явными заголовками секций в **объединенных ячейках**

#### Формат секции:

1. **Заголовок секции** (объединенная ячейка B:G, столбец C пуст)
   - **Ключевая особенность:** Заголовок находится в **объединенной ячейке** (диапазон B2:G2, B28:G28 и т.д.)
   - Формат: `"1. Цех по производству..."` или `"2. Склад..."`
   - Паттерн: `^\s*(\d+)\.?\s*(.*)$`
   - Примеры:
     - `"1. Цех по производству труб из полипропилена для холодного и горячего водоснабжения"` (B2:G2)
     - `"6. Склад готовой продукции"` (B98:G98)
     - `"10. Офис"` (B124:G124)
   - **Использование:** Название раздела в объединенной ячейке используется для определения типа оборудования и категории использования

2. **Заголовок таблицы** (строка после заголовка секции)
   - Столбец B: `"№"`
   - Столбец C: `"Оборудование"`
   - Столбцы D-G: `"Кол-во (шт)"`, `"Мощн-едн (кВт*ч)"`, `"Общ-мощн (кВт*ч)"`, `"ЧП (VFD)"`

3. **Список оборудования** (после заголовка таблицы)
   - Столбец B: Номер по порядку (1, 2, 3, ...)
   - Столбец C: Название оборудования
   - Столбец D: Количество (шт)
   - Столбец E: Мощность единицы (кВт)
   - Столбец F: Общая мощность (кВт)
   - Столбец G: ЧП (VFD) - `"+"` или `"-"`

4. **Итоговая строка** (в конце секции, объединенная ячейка B:C)
   - Столбец B: `"Итого"` (в объединенной ячейке B:C)
   - Столбец D: Общее количество
   - Столбец F: Общая мощность
   - Столбец G: Количество с ЧП
   - **Граница секции:** Объединенная ячейка "Итого" определяет конец текущей секции

#### Найденные секции на Лист1:

| № | Название секции | Строки | Оборудования | Итого (шт) | Мощность (кВт) | ЧП |
|---|----------------|--------|--------------|------------|----------------|-----|
| 1 | 1. Цех по производству труб из полипропилена для холодного и горячего водоснабжения | 2-26 | 22 | 26 | 2536.05 | 10 |
| 2 | 2. Цех по производству труб из ПВХ для канализационных систем | 28-52 | 22 | 23 | 771.7 | 8 |
| 3 | 3. Цех по производству фитингов из полипропилена и ПВХ | 54-73 | 17 | 32 | 1248.96 | 0 |
| 4 | 4. Цех по производству труб для напольного отопления | 75-82 | 5 | 5 | 364.45 | 3 |
| 5 | 5. Цех по гранулированию | 84-96 | 10 | 10 | 576.45 | 4 |
| 6 | 6. Склад готовой продукции | 98-102 | 2 | 13 | 1.95 | - |
| 7 | 7. Склад Сырья | 104-108 | 1 | - | - | - |
| 8 | 8. Склад для сырья и комплектующих материалов | 109-117 | 6 | 6 | 34.8 | 0 |
| 9 | 9. Склад готовой продукции для труб из ПВХ | 119-123 | 1 | - | - | - |
| 10 | 10. Офис | 124-131 | 5 | 14 | 37.85 | 0 |
| 11 | 11. Раздевалка, душевые | 133-139 | 4 | 6 | 14.3 | 0 |

**Итого на Лист1:** 11 секций, ~105 единиц оборудования

---

### Лист2 и Лист3

**Статус:** **ИГНОРИРУЮТСЯ** при парсинге

**Причина:** Сплошной список оборудования без структуры разделов и объединенных ячеек. Нет явных заголовков секций типа `"1. Цех..."`. Все оборудование в одной таблице.

**Решение:** Парсер обрабатывает только **Лист1**.

---

## 🔍 КЛЮЧЕВЫЕ ОСОБЕННОСТИ СТРУКТУРЫ

### 1. Определение заголовка секции

**Критерии:**
- Столбец B содержит текст
- Столбец C **пуст** (ключевое условие!)
- **Ячейка является объединенной** (диапазон B:G для заголовка секции)
- Текст соответствует паттерну `"N. Название..."` или содержит ключевые слова:
  - `"цех"`, `"склад"`, `"офис"`, `"раздевалка"`, `"душ"`, `"производству"`
  - `"помещение"`, `"котельная"`, `"подстанция"`, `"тп"`

**Важно:** 
- Номера оборудования (1, 2, 3...) НЕ являются заголовками секций, так как в столбце C есть название оборудования
- **Название раздела в объединенной ячейке используется для определения типа оборудования и категории использования электроэнергии**

### 2. Определение строки с оборудованием

**Критерии:**
- Столбец C содержит текст (название оборудования)
- Столбец B может содержать номер или быть пустым

### 3. Определение итоговой строки

**Критерии:**
- Столбец B содержит `"Итого"` (регистронезависимо)
- **Ячейка "Итого" является объединенной** (диапазон B:C)
- Обычно содержит агрегированные значения в столбцах D, F, G
- **Граница секции:** Объединенная ячейка "Итого" определяет конец текущей секции

---

## ⚠️ ПРОБЛЕМЫ И ОГРАНИЧЕНИЯ

### 1. Неоднородность структуры

- **Лист1:** Имеет правильную иерархическую структуру с явными секциями в объединенных ячейках
- **Лист2 и Лист3:** Не имеют явных заголовков секций, все оборудование в одной таблице
- **Решение:** Игнорировать Лист2 и Лист3, обрабатывать только Лист1

### 2. Парсер оборудования

Текущий парсер (`equipment_parser.py`) должен быть обновлен для работы с объединенными ячейками:

**Текущая логика:**
```python
def _is_section_title(label_cell: Optional[str], name_cell: Optional[str]) -> bool:
    if not label_cell:
        return False
    if name_cell:  # Если есть название оборудования - это НЕ секция
        return False
    if SECTION_TITLE_PATTERN.match(label_cell):
        return True
    keywords = ("цех", "склад", "офис", "раздевалка", "душ", "производству")
    lowered = label_cell.lower()
    return any(keyword in lowered for keyword in keywords)
```

**Рекомендации по улучшению:**
1. Проверять, является ли ячейка частью объединенного диапазона (B:G для заголовков секций)
2. Использовать название раздела из объединенной ячейки для определения категории оборудования
3. Игнорировать Лист2 и Лист3 при парсинге

### 3. Использование ИИ для классификации оборудования

**Для уточнения наименований и категорий использования электроэнергии:**

1. **Анализ названия раздела** (из объединенной ячейки):
   - Определяет контекст и тип помещения/цеха
   - Помогает классифицировать оборудование по категориям использования

2. **Анализ названия оборудования:**
   - Уточнение наименований оборудования
   - Определение категории использования электроэнергии:
     - `technological` - технологическое оборудование
     - `own_needs` - собственные нужды
     - `production` - производственное
     - `household` - хоз-бытовое

3. **Роль ИИ:**
   - Анализирует контекст (название раздела + название оборудования)
   - Присваивает соответствующую категорию использования
   - Уточняет и нормализует наименования оборудования

**Интеграция:**
- Использовать `domain/electricity_usage_classifier.py` с поддержкой ИИ
- Передавать в ИИ: название раздела, название оборудования, тип, место установки
- Получать от ИИ: уточненное наименование, категорию использования, дополнительные метаданные

---

## 📊 СТАТИСТИКА

| Лист | Секций | Оборудования | Общая мощность (кВт) | Статус |
|------|--------|--------------|----------------------|--------|
| Лист1 | 11 | ~105 | ~5586.66 | ✅ Используется |
| Лист2 | 0* | 135 | 6083.21 | ❌ Игнорируется |
| Лист3 | 0* | 116 | 6084.31 | ❌ Игнорируется |
| **Итого (обрабатывается)** | **11** | **~105** | **~5586.66** | |

*Лист2 и Лист3 не имеют явных секций и объединенных ячеек, поэтому игнорируются

### Статистика по объединенным ячейкам (Лист1):

- **Заголовков секций в объединенных ячейках:** 11 из 11 (100%)
- **Строк "Итого" в объединенных ячейках:** 11 из 11 (100%)
- **Формат объединенных ячеек:**
  - Заголовки секций: B2:G2, B28:G28, B54:G54, ... (столбцы B-G, одна строка)
  - Строки "Итого": B26:C26, B52:C52, B73:C73, ... (столбцы B-C, одна строка)

---

## 🔗 СВЯЗАННЫЕ МОДУЛИ

- `utils/equipment_parser.py` - Парсер оборудования
- `domain/electricity_usage_classifier.py` - Классификация оборудования по категориям использования
- `utils/canonical_collector.py` - Сбор канонических данных об оборудовании

---

## 📝 ПРИМЕРЫ СТРУКТУРЫ

### Пример 1: Правильная секция (Лист1)

```
Строка 2:  [B-G объединенная] "1. Цех по производству труб..."  [C] (пусто, часть объединенной)
Строка 3:  [B] "№"                                [C] "Оборудование"
Строка 4:  [B] "1"                                [C] "Трансформатор 100 (ТП-1)"
Строка 5:  [B] "2"                                [C] "Стабилизатор 300"
...
Строка 26: [B-C объединенная] "Итого"            [C] (пусто, часть объединенной)
```

**Особенности:**
- Заголовок секции в объединенной ячейке B2:G2
- Название раздела используется для определения категории оборудования
- Строка "Итого" в объединенной ячейке B26:C26 определяет конец секции

### Пример 2: Игнорируемая структура (Лист2, Лист3)

```
Строка 2:  [B] "№"                                [C] "Оборудование"
Строка 3:  [B] "1"                                [C] "Название оборудования 1"
Строка 4:  [B] "2"                                [C] "Название оборудования 2"
...
Строка 93: [B] "Итого"                           [C] (пусто)
```

**Проблема:** Нет заголовка секции в объединенной ячейке перед заголовком таблицы.  
**Решение:** Лист2 и Лист3 игнорируются при парсинге.

---

## 🤖 ИСПОЛЬЗОВАНИЕ ИИ ДЛЯ КЛАССИФИКАЦИИ

### Процесс классификации оборудования

1. **Извлечение данных из объединенной ячейки:**
   - Название раздела (например, "1. Цех по производству труб...")
   - Используется как контекст для классификации

2. **Анализ ИИ:**
   - **Входные данные:**
     - Название раздела (из объединенной ячейки)
     - Название оборудования
     - Тип оборудования (если указан)
     - Место установки (если указано)
   
   - **Выходные данные:**
     - Уточненное наименование оборудования
     - Категория использования электроэнергии:
       - `technological` - технологическое
       - `own_needs` - собственные нужды
       - `production` - производственное
       - `household` - хоз-бытовое
     - Дополнительные метаданные (мощность, назначение и т.д.)

3. **Интеграция:**
   - Модуль: `domain/electricity_usage_classifier.py`
   - Использование роли ИИ для анализа контекста
   - Сохранение результатов в `CanonicalSourceData`

---

## Конец анализа

