# 🎯 Система контроля готовности данных

## 📋 Описание

Система контроля готовности данных обеспечивает проверку полноты и корректности данных перед генерацией энергетического паспорта. Система включает:

1. **Матрицу обязательных данных** - конфигурацию требований к файлам
2. **Валидатор готовности** - модуль проверки готовности данных
3. **API endpoints** - для получения чек-листа и статуса готовности
4. **UI компоненты** - визуальный отображение готовности в интерфейсе

## 🚀 Компоненты

### 1. Матрица обязательных данных

**Файл:** `config/required_data_matrix.py`

Определяет требования к данным:
- Обязательные ресурсы: `electricity`, `gas`
- Опциональные ресурсы: `water`, `fuel`, `coal`, `heat`
- Минимальные требования: количество кварталов, поля данных

### 2. Валидатор готовности

**Файл:** `utils/readiness_validator.py`

**Функции:**
- `validate_generation_readiness(enterprise_id)` - проверка готовности данных
- `get_upload_checklist(enterprise_id)` - получение чек-листа файлов

**Возвращает:**
- `ready` - готовность к генерации (bool)
- `completeness_score` - показатель готовности (0.0-1.0)
- `missing_resources` - список недостающих ресурсов
- `missing_files` - список недостающих файлов
- `available_resources` - список доступных ресурсов
- `warnings` - список предупреждений
- `progress_percentage` - процент готовности

### 3. API Endpoints

**GET** `/api/enterprises/{enterprise_id}/upload-checklist`
- Возвращает чек-лист требуемых и опциональных файлов

**GET** `/api/enterprises/{enterprise_id}/generation-readiness`
- Возвращает статус готовности данных для генерации

**POST** `/api/generate-passport/{batch_id}?template_name={name}`
- Генерирует паспорт (с проверкой готовности)
- Параметр `skip_readiness_check=true` - пропустить проверку (для тестирования)

### 4. UI Компоненты

**Файл:** `web/results.html`

Визуальные элементы:
- **Progress bar** - индикатор готовности (0-100%)
- **Чек-лист** - список требуемых файлов со статусами
- **Предупреждения** - список проблем и недостающих данных
- **Блокировка кнопки** - кнопка генерации блокируется при неготовности

## 🔧 Использование

### Проверка готовности через API

```python
import requests

enterprise_id = 1

# Получить чек-лист
checklist = requests.get(f"/api/enterprises/{enterprise_id}/upload-checklist").json()

# Получить статус готовности
readiness = requests.get(f"/api/enterprises/{enterprise_id}/generation-readiness").json()

if readiness["ready"]:
    print(f"Данные готовы! Готовность: {readiness['progress_percentage']}%")
else:
    print(f"Данные не готовы. Отсутствуют: {readiness['missing_resources']}")
```

### Интеграция проверки в код

```python
from utils.readiness_validator import validate_generation_readiness

readiness = validate_generation_readiness(enterprise_id)

if not readiness["ready"]:
    raise ValueError(f"Данные не готовы: {readiness['missing_resources']}")

# Продолжить генерацию...
```

## ⚙️ Конфигурация

### Настройка минимальных требований

В `config/required_data_matrix.py`:

```python
MINIMAL_REQUIREMENTS = {
    "required_resources": ["electricity", "gas"],
    "min_quarters": 4,
    "min_completeness_score": 0.6  # Минимум 60% готовности
}
```

### Добавление нового ресурса

```python
REQUIRED_DATA_MATRIX = {
    "energy_resources": {
        "new_resource": {
            "required": True,
            "file_patterns": ["new_resource.xlsx"],
            "keywords": ["new", "resource"],
            "min_quarters": 4,
            "fields": ["field1", "field2"],
            "description": "Новый ресурс"
        }
    }
}
```

## 📊 Логика оценки готовности

Показатель готовности рассчитывается по формуле:

```
completeness = (
    required_score * 0.6 +    # Наличие обязательных ресурсов (60%)
    quarters_score * 0.3 +    # Количество кварталов (30%)
    optional_score * 0.1       # Опциональные ресурсы (10%)
)
```

**Готовность определяется как:**
- Все обязательные ресурсы присутствуют
- Показатель готовности >= 0.6 (60%)

## 🐛 Обработка ошибок

Система включает полную обработку ошибок:
- Все функции обернуты в try-except блоки
- Детальное логирование ошибок
- Graceful degradation - при ошибках возвращаются безопасные значения
- Пользовательские сообщения об ошибках в UI

## 📝 Логирование

Все операции логируются через стандартный logger:

```python
logger.info(f"Проверка готовности для предприятия {enterprise_id}")
logger.warning(f"Недостающие ресурсы: {missing_resources}")
logger.error(f"Ошибка при проверке: {e}", exc_info=True)
```

## ✅ Тестирование

Для тестирования генерации без проверки готовности:

```bash
POST /api/generate-passport/{batch_id}?skip_readiness_check=true
```

## 🎯 Критерии успеха

- ✅ Генерация блокируется при отсутствии обязательных файлов
- ✅ UI показывает четкий чек-лист требуемых файлов
- ✅ API возвращает детальную информацию о готовности
- ✅ Система определяет минимальный набор для генерации паспорта
- ✅ Пользователь видит, какие файлы еще нужно загрузить

---

**Версия:** 1.0  
**Дата:** 2025-01-15  
**Статус:** ✅ Реализовано и протестировано

