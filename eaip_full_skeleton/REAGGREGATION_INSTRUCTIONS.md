# 🔄 Инструкция по переагрегации данных

## Проблема

Если файлы были загружены **ДО** внесения исправлений в агрегатор, то данные могут быть агрегированы неправильно. В этом случае нужно **переагрегировать** существующие данные.

## Решение

### Вариант 1: Через API (рекомендуется)

1. **Найдите ID предприятия:**
   - Откройте веб-интерфейс
   - Посмотрите URL или используйте API: `GET /api/enterprises`

2. **Вызовите эндпоинт переагрегации:**
   ```bash
   curl -X POST http://localhost:8001/api/enterprises/{enterprise_id}/reaggregate
   ```

### Вариант 2: Через скрипт

```powershell
cd C:\eaip\eaip_full_skeleton\services\ingest
.\reaggregate.bat {enterprise_id}
```

## Что делает переагрегация?

1. Находит все загруженные файлы для предприятия
2. Извлекает raw_json из базы данных
3. Агрегирует данные заново с использованием исправленного кода
4. Сохраняет агрегированные данные в файлы JSON
5. Валидатор автоматически использует новые данные при следующей проверке

## После переагрегации

1. Обновите страницу готовности в веб-интерфейсе
2. Проверьте логи - должны быть сообщения `[DIAG]` о процессе агрегации
3. Попробуйте сгенерировать паспорт - данные должны быть готовы

