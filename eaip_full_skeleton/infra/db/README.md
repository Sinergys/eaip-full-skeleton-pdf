# 📊 База данных EAIP

## Структура БД

### Таблицы по сервисам:

#### 1. Ingest Service
- `ingest_batches` - информация о загруженных файлах
- `ingest_parsing_results` - результаты парсинга и распознавания

#### 2. Validate Service
- `validate_results` - результаты валидации данных

#### 3. Analytics Service
- `analytics_forecasts` - прогнозы и аналитика
- `analytics_data_points` - точки данных для анализа

#### 4. Gateway-Auth Service
- `users` - пользователи системы
- `user_sessions` - активные сессии пользователей

#### 5. Reports Service
- `reports` - сгенерированные отчеты

#### 6. Management Service
- `audit_records` - записи аудитов

#### 7. Recommend Service
- `recommendations` - рекомендации системы

#### 8. System
- `system_logs` - системные логи

## Инициализация

SQL скрипт `init.sql` автоматически выполняется при первом запуске PostgreSQL контейнера.

### Локально:
```bash
cd infra
docker compose up -d postgres
# Скрипт выполнится автоматически
```

### На сервере:
```bash
cd /opt/eaip/infra
docker compose -f docker-compose.staging.yml up -d postgres
# Скрипт выполнится автоматически
```

## Проверка создания таблиц

```bash
# Подключиться к БД
docker compose exec postgres psql -U eaip_user -d eaip_db

# Проверить таблицы
\dt

# Выйти
\q
```

## Миграции

Для будущих изменений схемы БД рекомендуется использовать:
- Alembic (для Python/SQLAlchemy)
- Или создавать новые SQL скрипты в `db/migrations/`

## Резервное копирование

```bash
# Создать бэкап
docker compose exec postgres pg_dump -U eaip_user eaip_db > backup.sql

# Восстановить
docker compose exec -T postgres psql -U eaip_user eaip_db < backup.sql
```

