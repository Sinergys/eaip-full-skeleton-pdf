# 🔍 Анализ ошибок валидации энергопаспорта (совместно с AI)

## 📋 Обнаруженные ошибки

### 1. `electricity.active_kwh` - отсутствует обязательное поле
**Лист:** 'Struktura pr2'  
**Описание:** Активное потребление электроэнергии по кварталам

### 2. `electricity.reactive_kvarh` - отсутствует обязательное поле
**Лист:** 'Struktura pr2'  
**Описание:** Реактивное потребление электроэнергии по кварталам

### 3. `electricity.by_usage` - отсутствует обязательное поле
**Лист:** '04_Баланс'  
**Описание:** Потребление электроэнергии по категориям (технологические, собственные нужды, производственные, хоз-бытовые)

### 4. `electricity.quarter_totals` - отсутствует обязательное поле
**Лист:** '05_Динамика'  
**Описание:** Квартальные итоги потребления электроэнергии

---

## 🤖 AI-анализ проблем

### Проблема 1: `electricity.active_kwh`

**Текущее состояние:**
- ✅ Добавлена функция `calculate_quarter_consumption_from_monthly_power()` в `energy_passport_calculations.py`
- ✅ Добавлена логика расчета в `_compute_quarter_totals()` в `energy_aggregator.py`
- ⚠️ **ПРОБЛЕМА:** Расчет срабатывает только если `active_kwh == 0`, но валидатор может не находить поле вообще

**Причины:**
1. Данные о мощности могут не извлекаться из исходных файлов
2. Поле может быть `None` вместо `0`, и условие `not totals.get("active_kwh") or totals.get("active_kwh") == 0` может не сработать
3. Валидатор ищет данные в структуре `resources.electricity.{quarter}.quarter_totals.active_kwh`, но данные могут быть в другом формате

**Решение:**
1. Улучшить извлечение данных о мощности из листа "ЭЛЕКТР"
2. Добавить fallback-расчет даже если поле отсутствует (не только если равно 0)
3. Убедиться, что `quarter_totals` всегда создается для каждого квартала

---

### Проблема 2: `electricity.reactive_kvarh`

**Текущее состояние:**
- ✅ Парсинг `reactive_kvarh` из листа "ЭЛЕКТР" реализован (колонка 3)
- ⚠️ **ПРОБЛЕМА:** Нет расчета из месячных данных реактивной мощности (аналогично `active_kwh`)

**Причины:**
1. Если в исходных данных нет `reactive_kvarh`, система не пытается его вычислить
2. Нет функции расчета реактивной энергии из мощности (аналогично активной)

**Решение:**
1. Добавить функцию `calculate_quarter_reactive_consumption_from_monthly_power()` 
2. Добавить логику расчета в `_compute_quarter_totals()` для `reactive_kvarh`
3. Поддержать различные названия полей реактивной мощности

---

### Проблема 3: `electricity.by_usage`

**Текущее состояние:**
- ✅ Функция `aggregate_usage_categories()` существует в `energy_aggregator.py`
- ✅ Функция `distribute_categories_by_quarter()` существует для распределения по кварталам
- ❌ **КРИТИЧЕСКАЯ ПРОБЛЕМА:** Эти функции **НЕ ВЫЗЫВАЮТСЯ** в `main.py`

**Причины:**
1. В `main.py:1003` вызывается только `aggregate_energy_data()`, но не `aggregate_usage_categories()`
2. Результат `aggregate_usage_categories()` не сохраняется и не интегрируется с агрегированными данными
3. Функция `distribute_categories_by_quarter()` никогда не вызывается

**Решение:**
1. Вызвать `aggregate_usage_categories()` после `aggregate_energy_data()` в `main.py`
2. Сохранить результат в `usage_categories.json` (как ожидает валидатор)
3. Вызвать `distribute_categories_by_quarter()` для добавления `by_usage` в агрегированные данные
4. Убедиться, что данные сохраняются в правильном формате

---

### Проблема 4: `electricity.quarter_totals`

**Текущее состояние:**
- ✅ Функция `_compute_quarter_totals()` создает `quarter_totals` для каждого квартала
- ⚠️ **ПРОБЛЕМА:** Валидатор может не находить данные из-за неправильной структуры

**Причины:**
1. Валидатор ожидает структуру `resources.electricity.{quarter}.quarter_totals`
2. Возможно, данные не сохраняются в правильном формате после агрегации
3. Возможно, `quarter_totals` создается пустым или с неправильными ключами

**Решение:**
1. Проверить, что `quarter_totals` всегда создается (даже если пустой)
2. Убедиться, что структура данных соответствует ожиданиям валидатора
3. Добавить логирование для отладки структуры данных

---

## 🛠️ План исправлений

### ✅ Приоритет 1 (Критично): Интеграция `by_usage` - **ВЫПОЛНЕНО**

1. ✅ **В `main.py` после `aggregate_energy_data()`:**
   - Добавлен вызов `aggregate_usage_categories(dst)`
   - Результат сохраняется в `usage_categories.json`
   - Вызван `distribute_categories_by_quarter()` для распределения по кварталам
   - Данные интегрированы в `aggregation_data`

### ✅ Приоритет 2 (Важно): Расчет `reactive_kvarh` - **ВЫПОЛНЕНО**

1. ✅ **Добавлена функция расчета в `energy_passport_calculations.py`:**
   - `calculate_quarter_reactive_consumption_from_monthly_power()` - расчет квартального реактивного потребления

2. ✅ **Добавлена логика в `_compute_quarter_totals()`:**
   - Поиск данных о реактивной мощности по месяцам
   - Расчет `reactive_kvarh` из месячных данных реактивной мощности
   - Поддержка различных названий полей реактивной мощности

### ✅ Приоритет 3 (Улучшение): Улучшение расчета `active_kwh` - **ВЫПОЛНЕНО**

1. ✅ **Улучшено условие проверки:**
   - Проверка на `None`, `0` и отсутствие значения
   - Используется: `active_kwh_value is None or active_kwh_value == 0 or not active_kwh_value`

2. ⚠️ **Извлечение данных о мощности из листа "ЭЛЕКТР"** - уже реализовано ранее

### ✅ Приоритет 4 (Проверка): Гарантировать `quarter_totals` - **ВЫПОЛНЕНО**

1. ✅ **Гарантировано наличие `quarter_totals`:**
   - Проверка и создание `quarter_totals` если отсутствует
   - Обновление существующих значений
   - Сохранение всех вычисленных значений

---

## 📊 Ожидаемый результат

После исправлений:

1. ✅ `electricity.active_kwh` - **РЕАЛИЗОВАНО:** рассчитывается из месячных данных мощности
2. ✅ `electricity.reactive_kvarh` - **РЕАЛИЗОВАНО:** рассчитывается из месячных данных реактивной мощности
3. ✅ `electricity.by_usage` - **РЕАЛИЗОВАНО:** извлекается из `pererashod.xlsx` и распределяется по кварталам
4. ✅ `electricity.quarter_totals` - **РЕАЛИЗОВАНО:** всегда присутствует в структуре данных

## ✅ Статус исправлений

**Все критические проблемы исправлены!**

### Выполненные изменения:

1. **`main.py`:**
   - Добавлены импорты `aggregate_usage_categories` и `distribute_categories_by_quarter`
   - Интегрирована обработка категорий использования после агрегации данных
   - Сохранение `usage_categories.json` для валидатора

2. **`energy_passport_calculations.py`:**
   - Добавлена функция `calculate_quarter_reactive_consumption_from_monthly_power()`

3. **`energy_aggregator.py`:**
   - Улучшена проверка для `active_kwh` (проверка на `None`)
   - Добавлен расчет `reactive_kvarh` из месячных данных реактивной мощности
   - Гарантировано наличие `quarter_totals` для всех кварталов
   - Улучшена обработка пустых значений

### Тестирование:

Рекомендуется протестировать с реальными данными:
1. Загрузить файл с данными об электроэнергии
2. Проверить, что все 4 поля присутствуют в валидации
3. Убедиться, что расчеты корректны

---

## 🔍 Дополнительные проверки

1. Проверить логику валидатора в `readiness_validator.py:730-792`
2. Проверить формат сохранения агрегированных данных
3. Добавить unit-тесты для новых функций расчета
4. Проверить обработку edge-cases (пустые данные, отсутствующие листы)

