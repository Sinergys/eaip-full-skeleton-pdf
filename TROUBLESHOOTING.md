# 🔧 Руководство по устранению неполадок проекта АТЛАС

## 📋 Содержание

1. [Общие проблемы](#общие-проблемы)
2. [Проблемы с сервисами](#проблемы-с-сервисами)
3. [Проблемы с базой данных](#проблемы-с-базой-данных)
4. [Проблемы с AI](#проблемы-с-ai)
5. [Проблемы с производительностью](#проблемы-с-производительностью)
6. [Проблемы с развертыванием](#проблемы-с-развертыванием)

---

## 🔴 Общие проблемы

### Проблема: Сервис не запускается

**Симптомы:**
- Контейнер постоянно перезапускается
- Ошибки в логах при запуске

**Решение:**
```bash
# 1. Проверьте логи
docker compose -f docker-compose.prod.yml logs <service_name>

# 2. Проверьте переменные окружения
docker compose -f docker-compose.prod.yml config

# 3. Проверьте зависимости
docker compose -f docker-compose.prod.yml ps

# 4. Пересоздайте контейнер
docker compose -f docker-compose.prod.yml up -d --force-recreate <service_name>
```

### Проблема: Сервис недоступен извне

**Симптомы:**
- Health check не проходит
- 502 Bad Gateway

**Решение:**
```bash
# 1. Проверьте что сервис запущен
docker compose -f docker-compose.prod.yml ps

# 2. Проверьте порты
netstat -tulpn | grep <port>

# 3. Проверьте сеть Docker
docker network ls
docker network inspect eaip_prod_network

# 4. Проверьте Nginx конфигурацию
docker compose -f docker-compose.prod.yml exec nginx nginx -t
```

### Проблема: Недостаточно памяти

**Симптомы:**
- OOM (Out of Memory) ошибки
- Контейнеры убиваются системой

**Решение:**
```bash
# 1. Проверьте использование памяти
docker stats

# 2. Увеличьте лимиты в docker-compose.prod.yml
deploy:
  resources:
    limits:
      memory: 4G  # Увеличьте лимит

# 3. Оптимизируйте настройки
# Уменьшите BATCH_MAX_WORKERS
# Уменьшите UVICORN_WORKERS
```

---

## 🗄️ Проблемы с базой данных

### Проблема: Не удается подключиться к PostgreSQL

**Симптомы:**
- Connection refused
- Authentication failed

**Решение:**
```bash
# 1. Проверьте что PostgreSQL запущен
docker compose -f docker-compose.prod.yml ps postgres

# 2. Проверьте логи
docker compose -f docker-compose.prod.yml logs postgres

# 3. Проверьте переменные окружения
docker compose -f docker-compose.prod.yml exec postgres env | grep POSTGRES

# 4. Проверьте подключение вручную
docker compose -f docker-compose.prod.yml exec postgres \
  psql -U $POSTGRES_USER -d $POSTGRES_DB -c "SELECT 1;"
```

### Проблема: Медленные запросы к БД

**Симптомы:**
- Долгая обработка запросов
- Таймауты

**Решение:**
```bash
# 1. Проверьте индексы
docker compose -f docker-compose.prod.yml exec postgres \
  psql -U $POSTGRES_USER -d $POSTGRES_DB -c "\d uploads"

# 2. Создайте недостающие индексы
CREATE INDEX IF NOT EXISTS idx_uploads_batch_id ON uploads(batch_id);
CREATE INDEX IF NOT EXISTS idx_uploads_enterprise_id ON uploads(enterprise_id);

# 3. Анализируйте медленные запросы
docker compose -f docker-compose.prod.yml exec postgres \
  psql -U $POSTGRES_USER -d $POSTGRES_DB -c "
    SELECT query, mean_exec_time, calls 
    FROM pg_stat_statements 
    ORDER BY mean_exec_time DESC 
    LIMIT 10;
  "

# 4. Увеличьте connection pool
POSTGRES_POOL_MAX=20
```

### Проблема: База данных переполнена

**Симптомы:**
- Недостаточно места на диске
- Ошибки записи

**Решение:**
```bash
# 1. Проверьте размер базы данных
docker compose -f docker-compose.prod.yml exec postgres \
  psql -U $POSTGRES_USER -d $POSTGRES_DB -c "
    SELECT pg_size_pretty(pg_database_size('$POSTGRES_DB'));
  "

# 2. Очистите старые данные
DELETE FROM uploads WHERE created_at < NOW() - INTERVAL '90 days';

# 3. VACUUM базу данных
docker compose -f docker-compose.prod.yml exec postgres \
  psql -U $POSTGRES_USER -d $POSTGRES_DB -c "VACUUM FULL;"
```

---

## 🤖 Проблемы с AI

### Проблема: AI API недоступен

**Симптомы:**
- Timeout ошибки
- API key invalid

**Решение:**
```bash
# 1. Проверьте API ключ
echo $DEEPSEEK_API_KEY  # или OPENAI_API_KEY, ANTHROPIC_API_KEY

# 2. Проверьте подключение к API
curl -H "Authorization: Bearer $DEEPSEEK_API_KEY" \
  https://api.deepseek.com/v1/models

# 3. Проверьте настройки в .env.prod
AI_ENABLED=true
AI_PROVIDER=deepseek
DEEPSEEK_API_KEY=your_key

# 4. Проверьте логи
docker compose -f docker-compose.prod.yml logs ingest | grep -i "ai\|deepseek"
```

### Проблема: Высокая стоимость AI API

**Симптомы:**
- Большие счета за API
- Превышение лимитов

**Решение:**
```bash
# 1. Включите кэширование
AI_CACHE_TTL_SECONDS=7200  # 2 часа

# 2. Используйте более дешевую модель
DEEPSEEK_MODEL=deepseek-chat  # Вместо gpt-4

# 3. Ограничьте использование AI
AI_PREFER_FOR_PDF=false  # Используйте только когда необходимо

# 4. Мониторьте использование
curl http://localhost:8001/metrics/ai/tokens
```

### Проблема: AI возвращает некорректные результаты

**Симптомы:**
- Неправильно распознанные данные
- Низкое качество анализа

**Решение:**
```bash
# 1. Проверьте промпты в коде
# Улучшите промпты для более точных результатов

# 2. Используйте более мощную модель
OPENAI_MODEL_TEXT=gpt-4
ANTHROPIC_MODEL=claude-3-opus-20240229

# 3. Увеличьте temperature для более креативных ответов
AI_TEMPERATURE=0.3

# 4. Проверьте входные данные
# Убедитесь что данные корректно форматируются перед отправкой в AI
```

---

## ⚡ Проблемы с производительностью

### Проблема: Медленная обработка файлов

**Симптомы:**
- Долгая обработка PDF/Excel
- Таймауты

**Решение:**
```bash
# 1. Включите батчевую обработку
BATCH_MAX_WORKERS=4
BATCH_SIZE=10

# 2. Оптимизируйте OCR
OCR_MAX_IMAGE_SIZE_MB=10.0

# 3. Увеличьте ресурсы для ingest сервиса
deploy:
  resources:
    limits:
      cpus: '4.0'
      memory: 4G

# 4. Используйте кэширование
AI_CACHE_TTL_SECONDS=3600
```

### Проблема: Высокое использование памяти

**Симптомы:**
- OOM ошибки
- Медленная работа системы

**Решение:**
```bash
# 1. Проверьте использование памяти
docker stats

# 2. Уменьшите размер батчей
BATCH_SIZE=5
BATCH_MAX_WORKERS=2

# 3. Оптимизируйте OCR
OCR_MAX_IMAGE_SIZE_MB=5.0

# 4. Очистите кэш
from utils.ai_cache import get_ai_cache
cache = get_ai_cache()
cache.clear()
```

### Проблема: Медленные запросы к API

**Симптомы:**
- Долгие ответы от сервисов
- Таймауты

**Решение:**
```bash
# 1. Проверьте connection pool
POSTGRES_POOL_MAX=10
SQLALCHEMY_POOL_SIZE=5

# 2. Оптимизируйте запросы к БД
# Добавьте индексы
# Используйте JOIN вместо множественных запросов

# 3. Используйте кэширование
# Redis для часто запрашиваемых данных

# 4. Увеличьте таймауты
REQUEST_TIMEOUT=600
```

---

## 🚀 Проблемы с развертыванием

### Проблема: Ошибки при сборке образов

**Симптомы:**
- Build failed
- Dependency errors

**Решение:**
```bash
# 1. Очистите кэш Docker
docker system prune -a

# 2. Пересоберите образы
docker compose -f docker-compose.prod.yml build --no-cache

# 3. Проверьте Dockerfile
# Убедитесь что все зависимости указаны

# 4. Проверьте requirements.txt
pip install -r requirements.txt
```

### Проблема: Проблемы с volumes

**Симптомы:**
- Permission denied
- Данные не сохраняются

**Решение:**
```bash
# 1. Проверьте права доступа
ls -la infra/data/

# 2. Исправьте права
chmod -R 755 infra/data
chown -R $USER:$USER infra/data

# 3. Проверьте mount points
docker compose -f docker-compose.prod.yml config | grep volumes

# 4. Пересоздайте volumes
docker compose -f docker-compose.prod.yml down -v
docker compose -f docker-compose.prod.yml up -d
```

### Проблема: SSL сертификат не работает

**Симптомы:**
- SSL errors
- Certificate expired

**Решение:**
```bash
# 1. Проверьте сертификат
openssl x509 -in infra/nginx/ssl/cert.pem -text -noout

# 2. Обновите сертификат Let's Encrypt
certbot renew

# 3. Скопируйте новый сертификат
cp /etc/letsencrypt/live/yourdomain.com/fullchain.pem infra/nginx/ssl/cert.pem
cp /etc/letsencrypt/live/yourdomain.com/privkey.pem infra/nginx/ssl/key.pem

# 4. Перезапустите Nginx
docker compose -f docker-compose.prod.yml restart nginx
```

---

## 📞 Получение помощи

### Сбор информации для диагностики

```bash
# Создайте диагностический отчет
./scripts/diagnostic.sh > diagnostic_report.txt
```

Скрипт собирает:
- Версии Docker и Docker Compose
- Статус всех контейнеров
- Логи последних ошибок
- Использование ресурсов
- Конфигурацию сети

### Полезные команды

```bash
# Просмотр всех логов
docker compose -f docker-compose.prod.yml logs --tail=100

# Проверка конфигурации
docker compose -f docker-compose.prod.yml config

# Статистика использования ресурсов
docker stats

# Проверка сети
docker network inspect eaip_prod_network

# Проверка volumes
docker volume ls
docker volume inspect <volume_name>
```

---

## ✅ Чеклист диагностики

При возникновении проблемы:

- [ ] Проверены логи всех сервисов
- [ ] Проверен статус контейнеров
- [ ] Проверены переменные окружения
- [ ] Проверены зависимости (БД, Redis, MinIO)
- [ ] Проверено использование ресурсов
- [ ] Проверена конфигурация сети
- [ ] Проверены права доступа к файлам
- [ ] Собран диагностический отчет

---

**Важно**: Всегда делайте резервную копию перед внесением изменений в продакшен!

