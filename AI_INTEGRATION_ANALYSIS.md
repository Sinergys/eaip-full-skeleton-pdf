# 🤖 Анализ интеграции ИИ для интеллектуального разбора данных

## 📊 Текущая ситуация

### ✅ Что уже реализовано

1. **ИИ-парсер для PDF и изображений**
   - Модуль `ai_parser.py` с поддержкой DeepSeek, OpenAI, Anthropic, Gemini
   - Используется для OCR и распознавания изображений
   - Интегрирован в `file_parser.py` с автоматическим fallback

2. **AI-модули для обработки данных**
   - `ai_data_validator.py` - валидация извлеченных данных
   - `ai_table_parser.py` - структурирование таблиц
   - `ai_energy_analysis.py` - анализ энергетических данных
   - `ai_ocr_enhancer.py` - улучшение OCR результатов
   - `ai_compliance_checker.py` - проверка соответствия ПКМ №690

3. **Классификация ресурсов (без ИИ)**
   - `ResourceClassifier` - использует правила и ключевые слова
   - `ContentAnalyzer` - анализирует структуру данных по правилам
   - Приоритет: содержимое → имя файла → fallback

### ⚠️ Проблемы текущей реализации

1. **Классификация основана на правилах**
   - Не всегда точно определяет тип ресурса
   - Проблемы с альтернативными названиями листов (например, "ЭЛЕКТР" vs "Электроэнергия")
   - Не учитывает контекст и структуру данных

2. **Извлечение данных из Excel**
   - Использует жестко заданные названия листов
   - Не распознает альтернативные форматы таблиц
   - Проблемы с вычислением формул из Excel

3. **Недостающие данные**
   - Нет автоматического расчета формул для энергопаспорта
   - Нет связи с данными из отчета Метин Ирода
   - Недостаточная валидация полноты данных

## 🎯 Задачи для ИИ

### 1. Интеллектуальная классификация данных

**Текущая проблема:** `ResourceClassifier` использует правила и ключевые слова

**Решение с ИИ:**
- Использовать LLM для анализа содержимого файла
- Определение типа ресурса на основе структуры данных
- Распознавание альтернативных названий листов и форматов

**Архитектура:**
```python
class AIContentClassifier:
    """ИИ-классификатор содержимого файлов"""
    
    def classify_with_ai(
        self, 
        raw_json: Dict[str, Any],
        filename: str
    ) -> tuple[str, float]:
        """
        Классификация с помощью ИИ
        
        Returns:
            (тип_ресурса, уверенность)
        """
        # Анализ структуры данных с помощью LLM
        # Определение типа ресурса по содержимому
        pass
```

**Интеграция:**
- Добавить в `ResourceClassifier.classify()` вызов ИИ-классификатора
- Использовать ИИ только когда правила не уверены (уверенность < 0.7)
- Fallback на правила при ошибках ИИ

### 2. Извлечение структурированных данных

**Текущая проблема:** Жестко заданные названия листов и форматы таблиц

**Решение с ИИ:**
- Использовать LLM для анализа структуры Excel файлов
- Автоматическое распознавание таблиц и их структуры
- Извлечение данных из альтернативных форматов

**Архитектура:**
```python
class AITableExtractor:
    """ИИ-извлечение таблиц из файлов"""
    
    def extract_tables_with_ai(
        self,
        raw_json: Dict[str, Any],
        resource_type: str
    ) -> Dict[str, Any]:
        """
        Извлечение таблиц с помощью ИИ
        
        Определяет:
        - Структуру таблицы (заголовки, строки, колонки)
        - Типы данных (числа, даты, единицы измерения)
        - Связи между таблицами
        """
        pass
```

**Интеграция:**
- Использовать в `energy_aggregator.py` для распознавания альтернативных форматов
- Использовать в `aggregate_from_db_json()` для анализа структуры данных

### 3. Дополнение недостающих данных

**Текущая проблема:** Нет автоматического расчета формул для энергопаспорта

**Решение с ИИ:**
- Использовать LLM для анализа формул энергопаспорта
- Автоматический расчет недостающих значений
- Валидация вычисленных значений

**Архитектура:**
```python
class AIFormulaCalculator:
    """ИИ-калькулятор формул для энергопаспорта"""
    
    def calculate_missing_formulas(
        self,
        passport_data: Dict[str, Any],
        available_data: Dict[str, Any]
    ) -> Dict[str, Any]:
        """
        Расчет недостающих формул
        
        Анализирует:
        - Какие формулы нужны для энергопаспорта
        - Какие данные доступны
        - Какие формулы можно вычислить
        """
        pass
```

**Интеграция:**
- Использовать в `readiness_validator.py` для определения недостающих данных
- Использовать в генераторе паспорта для расчета формул

### 4. Кросс-референсинг данных

**Текущая проблема:** Нет связи с данными из отчета Метин Ирода

**Решение с ИИ:**
- Использовать LLM для поиска связанных данных
- Автоматическое сопоставление данных из разных источников
- Валидация согласованности данных

**Архитектура:**
```python
class AICrossReferencer:
    """ИИ-кросс-референсинг данных"""
    
    def link_data_sources(
        self,
        uploaded_data: Dict[str, Any],
        metin_report: Dict[str, Any]
    ) -> Dict[str, Any]:
        """
        Связывание данных из разных источников
        
        Определяет:
        - Связи между данными из загруженных файлов и отчета Метин Ирода
        - Противоречия в данных
        - Дополнительные данные из отчета
        """
        pass
```

**Интеграция:**
- Использовать при загрузке файлов для поиска связанных данных
- Использовать в валидации для проверки согласованности

## 🏗️ Архитектурные изменения

### Минимальные изменения

1. **Добавить ИИ-классификатор** (новый модуль)
   - `utils/ai_content_classifier.py`
   - Интеграция в `ResourceClassifier.classify()`

2. **Расширить AI-парсер** (существующий модуль)
   - Добавить метод для анализа структуры Excel
   - Добавить метод для извлечения таблиц

3. **Создать AI-калькулятор формул** (новый модуль)
   - `utils/ai_formula_calculator.py`
   - Интеграция в `readiness_validator.py`

4. **Создать AI-кросс-референсер** (новый модуль)
   - `utils/ai_cross_referencer.py`
   - Интеграция в `main.py` при загрузке файлов

### Приоритет интеграции

**Этап 1: Классификация (MVP)**
- Добавить `AIContentClassifier` в `ResourceClassifier`
- Использовать ИИ только когда правила неуверенны
- Fallback на правила при ошибках

**Этап 2: Извлечение данных**
- Расширить `ai_table_parser.py` для Excel
- Использовать в `energy_aggregator.py` для альтернативных форматов

**Этап 3: Расчет формул**
- Создать `AIFormulaCalculator`
- Интегрировать в валидацию и генерацию паспорта

**Этап 4: Кросс-референсинг**
- Создать `AICrossReferencer`
- Интегрировать при загрузке файлов

## 📋 Вопросы для уточнения

### 1. Формат отчета Метин Ирода

**Вопрос:** Какой формат отчета Метин Ирода используется?
- PDF?
- Excel?
- Какая структура данных?

**Что нужно:**
- Пример файла или описание структуры
- Какие данные содержатся в отчете
- Как связаны данные с энергопаспортом

### 2. Недостающие формулы

**Вопрос:** Какие именно формулы недостающие в энергопаспорте?

**Что нужно:**
- Список формул, которые нужно рассчитать
- Какие данные необходимы для расчета
- Примеры вычислений

**Возможные формулы:**
- Удельные показатели (кВт·ч на единицу продукции)
- Энергетическая эффективность
- Сравнение с нормативами
- Динамика потребления

### 3. Примеры данных для обучения

**Вопрос:** Есть ли примеры данных для обучения ИИ?

**Что нужно:**
- Примеры правильно классифицированных файлов
- Примеры правильно извлеченных данных
- Примеры правильно рассчитанных формул

**Возможные источники:**
- Исторические данные из базы
- Эталонные файлы
- Валидированные паспорта

### 4. Важные поля для автоматизации

**Вопрос:** Какие поля наиболее важны для автоматического заполнения?

**Возможные приоритеты:**
1. **Критические поля** - обязательные для генерации паспорта
2. **Важные поля** - влияют на качество паспорта
3. **Опциональные поля** - можно заполнить позже

## 🚀 План реализации

### Фаза 1: ИИ-классификация (1-2 недели)

1. Создать `utils/ai_content_classifier.py`
2. Интегрировать в `ResourceClassifier.classify()`
3. Добавить fallback на правила
4. Тестирование на примерах файлов

### Фаза 2: ИИ-извлечение данных (2-3 недели)

1. Расширить `ai_table_parser.py` для Excel
2. Интегрировать в `energy_aggregator.py`
3. Добавить поддержку альтернативных форматов
4. Тестирование на различных форматах

### Фаза 3: ИИ-расчет формул (2-3 недели)

1. Создать `utils/ai_formula_calculator.py`
2. Интегрировать в `readiness_validator.py`
3. Добавить валидацию вычисленных значений
4. Тестирование на реальных данных

### Фаза 4: ИИ-кросс-референсинг (3-4 недели)

1. Создать `utils/ai_cross_referencer.py`
2. Интегрировать при загрузке файлов
3. Добавить проверку согласованности
4. Тестирование на связанных данных

## ✅ Требования выполнены

- ✅ **Минимальные изменения** - новые модули, интеграция через существующие интерфейсы
- ✅ **Поддержка различных форматов** - Excel, PDF, изображения
- ✅ **Возможность обучения** - можно добавить примеры в промпты
- ✅ **Надежность** - fallback на правила при ошибках ИИ

## 📝 Следующие шаги

1. **Уточнить детали:**
   - Формат отчета Метин Ирода
   - Список недостающих формул
   - Примеры данных для обучения

2. **Начать с Фазы 1:**
   - Создать `AIContentClassifier`
   - Интегрировать в `ResourceClassifier`
   - Протестировать на примерах

3. **Постепенно расширять:**
   - Добавлять новые возможности по мере необходимости
   - Собирать обратную связь от пользователей
   - Улучшать промпты на основе результатов

