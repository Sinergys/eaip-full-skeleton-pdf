"""Создание отчета о готовности к заполнению документов по пилоту Навои"""
from pathlib import Path
import json

project_root = Path(__file__).parent.parent
reports_dir = project_root / "reports" / "ocr"

# Загружаем статус БД
db_status_file = reports_dir / "database_status_check.json"
db_status = {}
if db_status_file.exists():
    with open(db_status_file, 'r', encoding='utf-8') as f:
        db_status = json.load(f)

content = f"""# ОТЧЕТ ЭКСПЕРТНОГО СОВЕТА: ГОТОВНОСТЬ К ЗАПОЛНЕНИЮ ДОКУМЕНТОВ ПО ПИЛОТУ НАВОИ

**Дата:** 2025-11-30
**Эксперты:** Data Scientist, Software Engineer, ML Engineer, QA Engineer
**Задача:** Оценка готовности к заполнению итоговых документов по пилоту Навои

---

## 1. ИМПОРТ ДАННЫХ В БАЗУ ДАННЫХ - ТЕКУЩИЙ ОБЪЕМ

### 1.1. Статус базы данных

**База данных:** SQLite (`ingest_data.db`)
**Расположение:** `eaip_full_skeleton/services/ingest/ingest_data.db`
**Статус:** {'✅ Существует' if db_status.get('db_exists') else '❌ Не найдена'}

### 1.2. Импортированные данные

#### Предприятия (Enterprises)
- **Количество:** {db_status.get('enterprises', 0)}
- **Статус:** {'✅ Данные импортированы' if db_status.get('enterprises', 0) > 0 else '❌ Нет данных'}
- **Детали:** {json.dumps(db_status.get('enterprises_list', [])[:5], ensure_ascii=False, indent=2) if db_status.get('enterprises_list') else 'Нет записей'}

#### Загруженные файлы (Uploads)
- **Количество:** {db_status.get('uploads', 0)}
- **Статус:** {'✅ Данные импортированы' if db_status.get('uploads', 0) > 0 else '❌ Нет данных'}
- **Детали:** {json.dumps(db_status.get('uploads_list', [])[:5], ensure_ascii=False, indent=2) if db_status.get('uploads_list') else 'Нет записей'}

#### Распарсенные данные (Parsed Data)
- **Количество:** {db_status.get('parsed_data', 0)}
- **Статус:** {'✅ Данные импортированы' if db_status.get('parsed_data', 0) > 0 else '❌ Нет данных'}
- **Детали:** {json.dumps(db_status.get('parsed_data_summary', [])[:5], ensure_ascii=False, indent=2) if db_status.get('parsed_data_summary') else 'Нет записей'}

### 1.3. Структура базы данных

**Таблицы:**
1. ✅ `enterprises` - предприятия
2. ✅ `uploads` - загруженные файлы
3. ✅ `parsed_data` - распарсенные данные (raw_json, editable_text)
4. ✅ `uploads_storage` - метаданные файлов (hash, mtime)
5. ✅ `normative_documents` - нормативные документы
6. ✅ `normative_rules` - правила из нормативных документов
7. ✅ `normative_references` - связи правил с полями паспорта

**PostgreSQL структура (infra/db/init.sql):**
- ✅ `ingest_batches` - батчи загрузки
- ✅ `ingest_parsing_results` - результаты парсинга (JSONB)
- ✅ `validate_results` - результаты валидации
- ⚠️ **Статус:** Структура создана, но данные могут быть не импортированы

---

## 2. ЧТО КРИТИЧЕСКИ ВАЖНО ДОБАВИТЬ В БАЗУ ПО ДАННЫМ

### 2.1. Критически важные данные для ПКМ 690 (по анализу Навои)

#### БЛОК 1: Данные предприятия (обязательно)
- ✅ **Базовая информация:** Название, ИНН, адрес, директор, отрасль
- ✅ **Статус:** Есть в структуре БД (`enterprises` таблица)
- ⚠️ **Проблема:** Нужна проверка полноты данных для Навои ИЭС

#### БЛОК 2: Энергоресурсы (критично)
- ⚠️ **Электроэнергия:** Годовое потребление, поквартальные данные, по узлам учёта
- ⚠️ **Газ:** Годовое потребление, поквартальные данные (есть только 1 квартал)
- ⚠️ **Вода:** Годовое потребление, поквартальные данные
- ⚠️ **Тепло:** Годовое потребление, поквартальные данные
- ⚠️ **Топливо/ГСМ:** Годовое потребление
- **Статус:** Есть агрегация (`energy_aggregator.py`), но нужно проверить импорт в БД

#### БЛОК 3: Оборудование (критично)
- ⚠️ **Список оборудования:** Электродвигатели, насосы, компрессоры
- ⚠️ **Характеристики:** Мощность, год выпуска, состояние, местоположение
- ⚠️ **Стоимость:** Стоимость оборудования, доставка, таможня, НДС, монтаж
- **Статус:** Есть парсер (`equipment_parser.py`), но нужно проверить импорт в БД

#### БЛОК 4: Узлы учёта (критично)
- ⚠️ **Список узлов:** Трансформаторные подстанции, точки учёта
- ⚠️ **Характеристики:** Тип, мощность, местоположение
- **Статус:** Есть парсер (`nodes_parser.py`), но нужно проверить импорт в БД

#### БЛОК 5: Оболочка зданий (рекомендуется)
- ⚠️ **U-значения:** Коэффициенты теплопередачи для элементов зданий
- ⚠️ **Площади:** Площади ограждающих конструкций
- **Статус:** Есть парсер (`building_envelope_parser.py`), но нужно проверить импорт в БД

#### БЛОК 6: Потери (критично для ТЭС)
- ⚠️ **Потери тепла:** Данные по потерям в теплосети
- ⚠️ **Потери электроэнергии:** Данные по потерям в сетях
- **Статус:** Есть файл "Навоий ИЭС потери по теплосети.xlsx", но нужно проверить импорт

#### БЛОК 7: Экологические данные (рекомендуется)
- ⚠️ **Выбросы:** Данные по атмосферным выбросам (есть за 3 года)
- **Статус:** Есть файл "Атмосфера маълумоти 3 йиллик.docx", но нужно проверить импорт

### 2.2. Критически недостающие данные (по анализу Навои)

#### КРИТИЧНО (блокирует заполнение):
1. **Данные коммерческого учёта электроэнергии:**
   - Детальные таблицы за период 2024-2025
   - По месяцам/кварталам
   - По узлам учёта (трансформаторным подстанциям)
   - Активная и реактивная энергия отдельно
   - **Статус:** ❌ Отсутствуют

2. **Данные коммерческого учёта газа:**
   - Полные данные за весь период (есть только 1 квартал)
   - По месяцам/кварталам
   - **Статус:** ⚠️ Частично (только 1 квартал)

3. **Акты балансов:**
   - Архив "акт баланс Навоий 2020-2025.rar" требует распаковки и обработки
   - **Статус:** ⚠️ Требуется обработка

#### ВАЖНО (рекомендуется для полноты):
4. **Проектная документация:**
   - Схемы, спецификации (PDF файлы)
   - Требует OCR/ручной обработки
   - **Статус:** ⚠️ Требуется обработка

5. **Данные по ГСМ:**
   - Полные данные за весь период
   - **Статус:** ⚠️ Частично

---

## 3. ПРОБЛЕМЫ, НЕДОДЕЛКИ, ВОПРОСЫ

### 3.1. КРИТИЧЕСКИЕ ПРОБЛЕМЫ (блокируют заполнение)

#### Проблема 1: Неполнота импорта данных в БД
- **Описание:** Данные парсятся и агрегируются, но не все импортируются в БД
- **Детали:**
  - Агрегированные данные (`*_aggregated.json`) сохраняются в файлы, но не в БД
  - Данные оборудования, узлов, оболочки парсятся, но не всегда сохраняются в БД
- **Влияние:** Невозможно автоматически заполнить документы ПКМ 690
- **Решение:** Реализовать импорт агрегированных данных в БД (таблица `ingest_parsing_results.structured_data`)

#### Проблема 2: Отсутствие критических данных
- **Описание:** Отсутствуют детальные данные коммерческого учёта электроэнергии и газа
- **Детали:**
  - Нет данных по узлам учёта электроэнергии
  - Нет полных данных по газу (только 1 квартал)
- **Влияние:** Невозможно заполнить разделы ПКМ 690 по энергоресурсам
- **Решение:** Запросить у пользователя недостающие данные

#### Проблема 3: Архив балансов не обработан
- **Описание:** Архив "акт баланс Навоий 2020-2025.rar" требует распаковки и обработки
- **Детали:** Архив содержит акты балансов за период 2020-2025
- **Влияние:** Отсутствуют данные для расчётов балансов
- **Решение:** Распаковать архив и обработать файлы

### 3.2. ВАЖНЫЕ ПРОБЛЕМЫ (затрудняют заполнение)

#### Проблема 4: Неполная интеграция OCR модуля
- **Описание:** OCR модуль улучшен (ШАГИ 1-7), но не интегрирован в основной пайплайн
- **Детали:**
  - Gemini Vision OCR работает через отдельные инструменты
  - Не используется в основном пайплайне обработки файлов
- **Влияние:** PDF файлы (счёт-фактуры, акты) не обрабатываются автоматически
- **Решение:** Интегрировать Gemini Vision OCR в `file_parser.py` (ШАГ 8.2)

#### Проблема 5: Отсутствие системы ручной идентификации
- **Описание:** Нет системы для ручной проверки проблемных документов
- **Детали:** Требования созданы, но система не реализована
- **Влияние:** Проблемные документы не могут быть обработаны
- **Решение:** Реализовать систему ручной идентификации (ШАГ 9)

#### Проблема 6: Неполная структура данных для ПКМ 690
- **Описание:** Структура данных не полностью соответствует требованиям ПКМ 690
- **Детали:**
  - Нет полей для категории энергоэффективности (A-G)
  - Нет полей для плана мероприятий
  - Нет полей для экологических данных
- **Влияние:** Невозможно заполнить все разделы ПКМ 690
- **Решение:** Расширить структуру данных в БД

### 3.3. ВОПРОСЫ ДЛЯ УТОЧНЕНИЯ

#### Вопрос 1: Какие данные уже импортированы в БД?
- **Статус:** Требуется проверка реального состояния БД
- **Действие:** Запустить скрипт проверки БД и анализ импортированных данных

#### Вопрос 2: Где хранятся агрегированные данные?
- **Статус:** Агрегированные данные сохраняются в файлы (`*_aggregated.json`)
- **Действие:** Проверить наличие файлов и их соответствие требованиям ПКМ 690

#### Вопрос 3: Какие файлы по Навои уже обработаны?
- **Статус:** Неизвестно, какие файлы из `C:\\AUDIT\\OBJECTS\\Navoiy IES\\INBOX` обработаны
- **Действие:** Проверить список обработанных файлов и их статус

#### Вопрос 4: Готовы ли шаблоны ПКМ 690?
- **Статус:** Шаблоны созданы (`templates/pcm690/`), но нужно проверить соответствие требованиям
- **Действие:** Проверить шаблоны и их соответствие ПКМ 690

#### Вопрос 5: Есть ли недостающие данные у пользователя?
- **Статус:** По анализу Навои выявлены недостающие данные
- **Действие:** Запросить у пользователя недостающие данные

---

## 4. ВЫВОДЫ И РЕКОМЕНДАЦИИ

### 4.1. Текущее состояние

**✅ Готово:**
- Структура БД создана
- Парсеры данных реализованы (энергоресурсы, оборудование, узлы, оболочка)
- Агрегация данных работает
- OCR модуль улучшен (ШАГИ 1-7)
- Шаблоны ПКМ 690 созданы

**⚠️ Требует внимания:**
- Импорт агрегированных данных в БД
- Интеграция OCR модуля в основной пайплайн
- Обработка архива балансов
- Проверка полноты данных для Навои

**❌ Критично:**
- Отсутствие детальных данных коммерческого учёта электроэнергии
- Отсутствие полных данных по газу (только 1 квартал)
- Неполнота импорта данных в БД

### 4.2. Рекомендации

#### НЕМЕДЛЕННО (перед заполнением документов):

1. **Проверить реальное состояние БД:**
   - Запустить скрипт проверки БД
   - Проанализировать импортированные данные
   - Определить, какие данные есть, а каких нет

2. **Импортировать агрегированные данные в БД:**
   - Найти все файлы `*_aggregated.json`
   - Импортировать их в таблицу `ingest_parsing_results.structured_data`
   - Проверить полноту данных

3. **Обработать архив балансов:**
   - Распаковать "акт баланс Навоий 2020-2025.rar"
   - Обработать файлы из архива
   - Импортировать данные в БД

4. **Запросить недостающие данные:**
   - Детальные данные коммерческого учёта электроэнергии
   - Полные данные по газу за весь период
   - Другие недостающие данные

#### КРАТКОСРОЧНО (1-2 недели):

5. **Интегрировать OCR модуль:**
   - Интегрировать Gemini Vision OCR в основной пайплайн (ШАГ 8.2)
   - Обработать PDF файлы (счёт-фактуры, акты)

6. **Реализовать систему ручной идентификации:**
   - Создать систему для ручной проверки проблемных документов (ШАГ 9)

7. **Расширить структуру данных:**
   - Добавить поля для категории энергоэффективности
   - Добавить поля для плана мероприятий
   - Добавить поля для экологических данных

### 4.3. Критерии готовности к заполнению

**Минимальные требования:**
- ✅ Данные предприятия импортированы
- ✅ Данные энергоресурсов импортированы (хотя бы частично)
- ✅ Данные оборудования импортированы
- ✅ Шаблоны ПКМ 690 готовы

**Рекомендуемые требования:**
- ✅ Все данные импортированы в БД
- ✅ OCR модуль интегрирован
- ✅ Система ручной идентификации работает
- ✅ Все недостающие данные получены

---

## 5. ПЛАН ДЕЙСТВИЙ

### Этап 1: Диагностика (1 день)
1. Проверить реальное состояние БД
2. Проанализировать импортированные данные
3. Определить недостающие данные

### Этап 2: Импорт данных (2-3 дня)
1. Импортировать агрегированные данные в БД
2. Обработать архив балансов
3. Импортировать данные оборудования, узлов, оболочки

### Этап 3: Получение недостающих данных (1-2 недели)
1. Запросить у пользователя недостающие данные
2. Обработать полученные данные
3. Импортировать в БД

### Этап 4: Интеграция и тестирование (1 неделя)
1. Интегрировать OCR модуль
2. Реализовать систему ручной идентификации
3. Протестировать заполнение документов

---

**СТАТУС:** ⚠️ **НЕ ГОТОВО К ЗАПОЛНЕНИЮ** - требуется выполнение этапов 1-3

**Следующий шаг:** Выполнить диагностику (Этап 1) для определения реального состояния БД и данных.
"""

output_file = reports_dir / "NAVOIY_READINESS_REPORT.md"
output_file.parent.mkdir(parents=True, exist_ok=True)

with open(output_file, 'w', encoding='utf-8') as f:
    f.write(content)

print(f"✅ Отчет о готовности создан: {output_file}")

