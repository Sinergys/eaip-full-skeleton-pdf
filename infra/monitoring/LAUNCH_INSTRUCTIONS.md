# 🚀 Инструкция по запуску Production Observability Bundle

## Быстрый старт

### На production сервере выполните:

```bash
# 1. Подключитесь к серверу
ssh user@prod-host

# 2. Перейдите в директорию
cd /opt/eaip/infra/monitoring

# 3. Убедитесь, что файлы на месте
ls -la

# 4. Создайте .env.prod.monitoring (если еще не создан)
cp .env.prod.monitoring.example .env.prod.monitoring

# 5. Отредактируйте переменные
nano .env.prod.monitoring
# Обязательно обновите:
# - DOMAIN=your-domain.com
# - GF_SECURITY_ADMIN_PASSWORD=$(openssl rand -base64 24)
# - TELEGRAM_BOT_TOKEN=your_token
# - TELEGRAM_CHAT_ID=your_chat_id

# 6. Запустите скрипт развертывания
chmod +x launch-prod-observability.sh
bash launch-prod-observability.sh
```

## Что делает скрипт

1. ✅ Проверяет и создает необходимые директории
2. ✅ Обрабатывает шаблон Alertmanager с переменными
3. ✅ Создает Docker сеть monitoring
4. ✅ Настраивает firewall (открывает 80/443, закрывает внутренние порты)
5. ✅ Запускает все сервисы мониторинга
6. ✅ Подключает app-контейнеры к сети monitoring
7. ✅ Выполняет smoke tests
8. ✅ Предлагает протестировать алерты
9. ✅ Предлагает закоммитить изменения в git

## Ручное выполнение (если нужно)

Если хотите выполнить шаги вручную, следуйте инструкциям в промпте или используйте `deploy-prod-monitoring.sh`.

## После запуска

1. Откройте https://your-domain.com
2. Войдите в Grafana (admin/<пароль из .env>)
3. Смените пароль администратора
4. Проверьте, что dashboards загружены
5. Проверьте, что datasources настроены (Prometheus, Loki)
6. Протестируйте алерты, временно остановив сервис

## Troubleshooting

### SSL сертификат не выдается
- Проверьте, что домен указывает на сервер: `dig your-domain.com`
- Проверьте логи Caddy: `docker logs caddy-prod`
- Убедитесь, что порты 80 и 443 открыты

### Сервисы не доступны
- Проверьте статус: `docker compose -f docker-compose.prod.monitoring.yml ps`
- Проверьте логи: `docker compose -f docker-compose.prod.monitoring.yml logs`

### Алерты не работают
- Проверьте Telegram токены в `.env.prod.monitoring`
- Проверьте логи Alertmanager: `docker logs alertmanager-prod`
- Убедитесь, что шаблон обработан: `cat alertmanager/alertmanager.yml | grep telegram`

---

**Готово к запуску!** 🚀

