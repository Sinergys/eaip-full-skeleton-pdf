# 🔄 Инструкция по перезапуску сервиса с DEBUG логированием

## Быстрый перезапуск

```powershell
cd C:\eaip\eaip_full_skeleton\services\ingest
.\restart_debug.bat
```

## Ручной перезапуск

1. Остановите текущий процесс (Ctrl+C)
2. Запустите с DEBUG:
```powershell
cd C:\eaip\eaip_full_skeleton\services\ingest
$env:LOG_LEVEL="DEBUG"
uvicorn main:app --reload --port 8001 --log-level debug
```

## Проверка логов

Ищите сообщения с префиксом `[DIAG]` для диагностической информации.

