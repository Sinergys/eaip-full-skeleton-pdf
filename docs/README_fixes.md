# Исправления типов и качества кода

Исправлены типы в: `ocr_data_adapter.py`, `ocr_table_extractor.py`, `enterprise_classifier.py`, `data_validator.py`, `database.py`.

Исключён `.venv` из линтинга.

Код теперь стабильнее — меньше шума, больше фокуса.

