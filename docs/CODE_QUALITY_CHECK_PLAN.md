# План проверок качества кода

## Приоритеты проверок

### 🔴 КРИТИЧЕСКИЕ (делать всегда)

#### 1. Линтинг (ruff check) - **ОБЯЗАТЕЛЬНО**
**Когда делать:**
- Перед каждым коммитом
- Перед созданием Pull Request
- После каждого значимого изменения кода
- В CI/CD пайплайне (автоматически)

**Почему важно:**
- Находит реальные ошибки в коде
- Неиспользуемые переменные, импорты
- Потенциальные баги
- Проблемы с производительностью

**Команда:**
```bash
ruff check services/reports/energy_passport/
```

**Статус:** ✅ Уже делаем, нашли и исправили 5 ошибок

---

#### 2. Форматирование (ruff format) - **ОБЯЗАТЕЛЬНО**
**Когда делать:**
- Перед каждым коммитом
- После каждого изменения кода
- В pre-commit hook (автоматически)

**Почему важно:**
- Единый стиль кода
- Легче читать и поддерживать
- Меньше конфликтов при слиянии

**Команда:**
```bash
ruff format services/reports/energy_passport/
```

**Статус:** ✅ Уже сделано, отформатировано 84 файла

---

### 🟡 РЕКОМЕНДУЕМЫЕ (делать регулярно)

#### 3. Проверка типов (mypy) - **РЕКОМЕНДУЕТСЯ**
**Когда делать:**
- Перед релизом
- При добавлении новых функций
- Раз в неделю для всего проекта
- В CI/CD пайплайне (опционально)

**Почему важно:**
- Находит ошибки типов до запуска
- Улучшает качество кода
- Помогает IDE с автодополнением
- Документирует код через типы

**Команда:**
```bash
mypy services/reports/energy_passport/ --ignore-missing-imports
```

**Установка:**
```bash
pip install mypy
```

**Статус:** ⚠️ Не установлен, но рекомендуется

**Пример проблем, которые находит:**
```python
# mypy найдет эту ошибку:
def calculate_total(items: List[int]) -> int:
    return sum(items)

result = calculate_total(["a", "b"])  # Ошибка: ожидается List[int]
```

---

#### 4. Безопасность (bandit) - **РЕКОМЕНДУЕТСЯ**
**Когда делать:**
- Перед релизом
- При работе с пользовательским вводом
- При работе с базами данных
- Раз в месяц для всего проекта

**Почему важно:**
- Находит уязвимости безопасности
- SQL-инъекции, XSS
- Небезопасное использование eval/exec
- Хардкод паролей и ключей

**Команда:**
```bash
bandit -r services/reports/energy_passport/
```

**Установка:**
```bash
pip install bandit
```

**Статус:** ⚠️ Не установлен, но рекомендуется

**Пример проблем, которые находит:**
```python
# bandit найдет эту уязвимость:
query = f"SELECT * FROM users WHERE id = {user_id}"  # SQL injection!
exec(user_input)  # Code injection!
password = "hardcoded_password_123"  # Hardcoded password
```

---

### 🟢 ОПЦИОНАЛЬНЫЕ (делать по необходимости)

#### 5. Анализ сложности (radon) - **ОПЦИОНАЛЬНО**
**Когда делать:**
- При рефакторинге
- Когда код становится сложным
- При code review сложных функций
- Раз в квартал

**Почему полезно:**
- Находит слишком сложные функции
- Помогает рефакторить код
- Улучшает поддерживаемость

**Команда:**
```bash
radon cc services/reports/energy_passport/ -a
radon mi services/reports/energy_passport/  # Индекс поддерживаемости
```

**Установка:**
```bash
pip install radon
```

**Статус:** ⚠️ Не установлен, опционально

---

#### 6. Документация (pydocstyle) - **ОПЦИОНАЛЬНО**
**Когда делать:**
- При добавлении новых функций
- Перед релизом публичного API
- По требованию команды

**Почему полезно:**
- Проверяет наличие docstrings
- Проверяет качество документации
- Улучшает понимание кода

**Команда:**
```bash
pydocstyle services/reports/energy_passport/
```

**Установка:**
```bash
pip install pydocstyle
```

**Статус:** ⚠️ Не установлен, опционально

---

## Рекомендуемый график проверок

### Ежедневно (автоматически)
- ✅ Форматирование (pre-commit hook)
- ✅ Линтинг (pre-commit hook)

### Перед коммитом (вручную или автоматически)
- ✅ Форматирование
- ✅ Линтинг

### Перед Pull Request
- ✅ Форматирование
- ✅ Линтинг
- 🟡 Проверка типов (mypy) - если установлен

### Перед релизом
- ✅ Форматирование
- ✅ Линтинг
- 🟡 Проверка типов (mypy)
- 🟡 Безопасность (bandit)
- 🟢 Анализ сложности (radon) - если нужно

### Раз в неделю
- 🟡 Проверка типов для всего проекта

### Раз в месяц
- 🟡 Безопасность для всего проекта

---

## Настройка автоматических проверок

### Pre-commit hooks

Создайте файл `.pre-commit-config.yaml`:

```yaml
repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.1.0
    hooks:
      - id: ruff
        args: [--fix]
      - id: ruff-format
  
  - repo: https://github.com/pre-commit/mirrors-mypy
    rev: v1.5.1
    hooks:
      - id: mypy
        args: [--ignore-missing-imports]
        additional_dependencies: [types-all]
```

Установка:
```bash
pip install pre-commit
pre-commit install
```

### CI/CD интеграция

Пример для GitHub Actions:

```yaml
name: Code Quality

on: [push, pull_request]

jobs:
  quality:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-python@v4
      
      - name: Install dependencies
        run: pip install ruff mypy bandit
      
      - name: Format check
        run: ruff format --check .
      
      - name: Lint
        run: ruff check .
      
      - name: Type check
        run: mypy . --ignore-missing-imports || true
      
      - name: Security check
        run: bandit -r . || true
```

---

## Текущий статус проекта

### ✅ Сделано
- [x] Форматирование - отформатировано 84 файла
- [x] Линтинг - найдено и исправлено 5 ошибок
- [x] Создан скрипт `tools/check_all_quality.py` для комплексной проверки

### 🟡 Рекомендуется сделать
- [ ] Установить mypy и настроить проверку типов
- [ ] Установить bandit и проверить безопасность
- [ ] Настроить pre-commit hooks для автоматических проверок
- [ ] Добавить проверки в CI/CD пайплайн

### 🟢 Опционально
- [ ] Установить radon для анализа сложности
- [ ] Установить pydocstyle для проверки документации

---

## Выводы и рекомендации

### Что делать СЕЙЧАС:
1. ✅ **Продолжать использовать линтинг и форматирование** - это критично
2. 🟡 **Установить mypy** - поможет найти ошибки типов
3. 🟡 **Установить bandit** - важно для безопасности
4. 🟡 **Настроить pre-commit hooks** - автоматизирует проверки

### Что делать ПОТОМ:
1. 🟢 Настроить CI/CD с автоматическими проверками
2. 🟢 Периодически запускать анализ сложности
3. 🟢 Проверять документацию при необходимости

### Приоритет:
1. **Критично:** Линтинг + Форматирование (уже делаем ✅)
2. **Важно:** Проверка типов + Безопасность (рекомендуется добавить)
3. **Полезно:** Сложность + Документация (опционально)

---

## Быстрый старт

### Минимальный набор (обязательно):
```bash
# Форматирование
ruff format services/reports/energy_passport/

# Линтинг
ruff check services/reports/energy_passport/
```

### Расширенный набор (рекомендуется):
```bash
# Установить инструменты
pip install mypy bandit

# Проверка типов
mypy services/reports/energy_passport/ --ignore-missing-imports

# Безопасность
bandit -r services/reports/energy_passport/
```

### Комплексная проверка:
```bash
python tools/check_all_quality.py
```

