# Классификация использования электроэнергии по категориям

**Дата:** 2025-01-XX  
**Модуль:** `domain/electricity_usage_classifier.py`

---

## 📋 ОБЗОР

Детерминистическая процедура классификации оборудования по категориям использования электроэнергии для расчета распределения стоимости в листе "Баланс".

## 🎯 КАТЕГОРИИ ИСПОЛЬЗОВАНИЯ

### 1. Технологические (`technological`)

**Описание:** Оборудование, непосредственно участвующее в технологическом процессе производства.

**Ключевые слова (RU/UZ):**

- `технолог`, `технологический`, `технология`
- `агрегат`, `аппарат`
- `насос` (технологический)
- `компрессор`
- `печь`, `печной`
- `реактор`
- `сушилка`
- `экструдер`
- `станок` (производственный)
- `линия` (производственная)

**Примеры значений:**

- Название: "Технологический насос", "Печь обжига", "Компрессор технологический"
- Тип: "Технологический агрегат"
- Секция: "1. Цех технологический"

### 2. Собственные нужды (`own_needs`)

**Описание:** Оборудование для обеспечения работы предприятия (котельные, подстанции, насосы собственных нужд).

**Ключевые слова (RU/UZ):**

- `собственные нужды`, `с.н.`, `собств. нужды`
- `котельная`, `котел`, `котёл`
- `подстанция`, `тп`, `тп-`, `трансформатор`
- `насос` (в контексте котельной/подстанции)
- `вентиляция` (общеобменная)
- `освещение` (производственное, не бытовое)

**Примеры значений:**

- Название: "Насос котельной собственных нужд", "ТП-1", "Котел собственных нужд"
- Место установки: "Котельная", "Подстанция"
- Секция: "Котельная собственных нужд"

### 3. Производственные (`production`)

**Описание:** Оборудование общего производственного назначения (цеха, участки, линии).

**Ключевые слова (RU/UZ):**

- `производств`, `производственный`
- `цех`, `цех №`, `цех-`
- `участок`, `участок №`
- `линия` (производственная)
- `конвейер`
- `станок` (общего назначения)
- `механизм`

**Примеры значений:**

- Название: "Насос цеха №1", "Конвейер участка №2"
- Место установки: "Цех №3", "Участок сборки"
- Секция: "1. Цех производства", "2. Участок упаковки"

**Примечание:** Это категория по умолчанию, если не удалось определить более специфичную категорию.

### 4. Хоз-бытовые (`household`)

**Описание:** Оборудование для административных и бытовых нужд.

**Ключевые слова (RU/UZ):**

- `хоз-быт`, `хозбыт`, `хозяйственно-бытовые`
- `бытовые`, `быт`
- `офис`, `офисный`, `административный`
- `админ`, `администрация`
- `склад` (не производственный)
- `раздевалка`, `душ`, `санузел`
- `освещение` (бытовое, офисное)
- `кондиционер` (офисный)

**Примеры значений:**

- Название: "Кондиционер офисный", "Освещение административного корпуса"
- Место установки: "Офис", "Административный корпус", "Склад готовой продукции"
- Секция: "Офис", "Административный корпус"

---

## 🔍 ОБНАРУЖЕННЫЕ ПОЛЯ ДЛЯ КЛАССИФИКАЦИИ

### Из `equipment_parser.py`

**Парсируемые поля:**

- `name` (строки 172) - Наименование оборудования
- `type` - Тип оборудования (если есть отдельная колонка)
- `location` - Место установки (если есть отдельная колонка)
- `section.name` (строки 136) - Название секции (например, "1. Цех ...")
- `section.title` (строки 128) - Полное название секции

**Структура секций:**

- Секции определяются по паттерну `"1. Цех ..."` (строка 131)
- Ключевые слова для секций: `("цех", "склад", "офис", "раздевалка", "душ", "производству")` (строка 203)

### Из `EquipmentItem` (CanonicalSourceData)

**Поля модели:**

- `name: str` - Наименование
- `type: Optional[str]` - Тип оборудования
- `model: Optional[str]` - Модель
- `location: Optional[str]` - Место установки
- `notes: Optional[str]` - Примечания
- `extra: Dict[str, Any]` - Дополнительные поля (может содержать `usage_category`, `назначение`, `purpose`)

### Потенциальные колонки в Excel (не всегда присутствуют)

**RU названия:**

- `Назначение` - прямое указание назначения
- `Примечание` - может содержать информацию о категории
- `Цех` / `Участок` - место установки
- `Узел` - привязка к узлу учета
- `Категория` - прямая категория использования

**UZ названия:**

- `Maqsad` - назначение
- `Eslatma` - примечание
- `Sex` / `Uchastok` - цех/участок

---

## 🎯 ПРАВИЛА КЛАССИФИКАЦИИ

### Приоритет 1: Явное указание в extra/metadata

1. Проверяем `item.extra.get("usage_category")`
2. Проверяем `item.extra.get("назначение")`
3. Проверяем `item.extra.get("purpose")`
4. Проверяем `item.extra.get("category")`
5. Нормализуем значение через `ELECTRICITY_USAGE_KEYWORDS`

### Приоритет 2: Анализ по ключевым словам

1. Ищем ключевые слова в `item.name` (нижний регистр)
2. Ищем ключевые слова в `item.type` (нижний регистр)
3. Ищем ключевые слова в `item.location` (нижний регистр)
4. Если несколько категорий совпадают, применяем приоритет:

   - `technological` > `own_needs` > `production` > `household`

### Приоритет 3: Анализ по узлам учета (опционально)

1. Если `nodes` предоставлены, пытаемся сопоставить `item.location` с `NodeItem.location`
2. Если узел находится в котельной/подстанции → `own_needs`
3. Если узел в производственном цехе → `production`
4. Если узел в административном корпусе → `household`

### Приоритет 4: Значение по умолчанию

- Если ничего не найдено → `production`

---

## ⚠️ ОГРАНИЧЕНИЯ И TODOS

### Текущие ограничения

1. **Нет явной колонки "Назначение"** в большинстве файлов оборудования
   - Решение: полагаемся на анализ названий и секций
   - TODO: Добавить поддержку дополнительных колонок при их обнаружении

2. **Неоднозначность категорий:**
   - Насос может быть технологическим, для собственных нужд или производственным
   - Решение: приоритет по контексту (название секции, location)
   - TODO: Улучшить эвристики для насосов и компрессоров

3. **Отсутствие данных о фактическом потреблении:**
   - Используем только номинальную мощность и коэффициент использования
   - TODO: При наличии данных о фактическом потреблении использовать их для весов

4. **Не учитывается сезонность:**
   - Распределение статично на весь год
   - TODO: Добавить поддержку квартального распределения при наличии данных

### Планы на будущее

1. Добавить машинное обучение для улучшения классификации
2. Поддержка пользовательских правил классификации
3. Интеграция с нормативными документами для определения категорий
4. Валидация распределения (сумма должна быть ≈ 100%)

---

## 📊 ПРИМЕРЫ КЛАССИФИКАЦИИ

### Пример 1: Технологическое оборудование

```python
EquipmentItem(
    name="Технологический насос ПН-100",
    type="Насос технологический",
    location="Цех №1",
    nominal_power_kw=50.0,
    utilization_factor=0.9
)
# Результат: "technological"
```

### Пример 2: Собственные нужды

```python
EquipmentItem(
    name="Насос котельной",
    type="Насос",
    location="Котельная",
    nominal_power_kw=30.0,
    extra={"usage_category": "собственные нужды"}
)
# Результат: "own_needs"
```

### Пример 3: Производственное (по умолчанию)

```python
EquipmentItem(
    name="Конвейер",
    type="Конвейер",
    location="Цех №2",
    nominal_power_kw=15.0
)
# Результат: "production" (по умолчанию, т.к. нет специфичных ключевых слов)
```

### Пример 4: Хоз-бытовое

```python
EquipmentItem(
    name="Кондиционер",
    type="Сплит-система",
    location="Офис",
    nominal_power_kw=3.5
)
# Результат: "household"
```

---

## 🔗 СВЯЗАННЫЕ МОДУЛИ

- `domain/passport_field_map.py` - Конфигурация ключевых слов и категорий
- `domain/electricity_usage_classifier.py` - Реализация классификатора
- `utils/canonical_to_passport.py` - Использование классификатора для расчета by_usage
- `main.py` - Интеграция в процесс генерации паспорта

---

## Конец документации
