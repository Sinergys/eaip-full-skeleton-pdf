# Архитектура AI-системы для будущего перехода на локальные модели

## Обзор

Создана архитектура, которая поддерживает как внешние API-провайдеры (OpenAI, Anthropic, DeepSeek), так и локальные модели. Это позволяет плавно перейти на локальные модели в будущем без изменения существующего кода.

## Архитектура

### Базовые компоненты

1. **`BaseAIClient`** - Абстрактный базовый класс
   - Единый интерфейс для всех AI-клиентов
   - Методы: `process_prompt()`, `process_vision_prompt()`, `get_usage_metrics()`, `is_available()`

2. **`AIClientFactory`** - Фабрика для создания клиентов
   - Автоматический выбор провайдера
   - Поддержка режима "auto" (сначала локальный, потом API)

3. **API-клиенты** (наследуются от `BaseAIClient`):
   - `OpenAIClient` - OpenAI API
   - `AnthropicClient` - Anthropic (Claude) API
   - `DeepSeekClient` - DeepSeek API

4. **`LocalAIClient`** - Заглушка для локальных моделей
   - Готова к реализации в будущем
   - Пока возвращает ошибку при попытке использования

5. **`ai_config.py`** - Конфигурация системы
   - Настройки провайдеров
   - Пути к локальным моделям
   - Параметры по умолчанию

## Структура файлов

```
services/ingest/
├── ai_base_client.py          # Базовый абстрактный класс
├── ai_client_factory.py       # Фабрика для создания клиентов
├── ai_api_clients.py          # Реализации для внешних API
├── ai_local_client.py         # Заглушка для локальных моделей
├── ai_config.py               # Конфигурация системы
└── ai_parser.py               # Существующий парсер (пока не обновлен)
```

## Использование

### Создание клиента через фабрику

```python
from ai_client_factory import AIClientFactory

# Автоматический выбор (сначала локальный, потом API)
client = AIClientFactory.create_client(client_type="auto")

# Явное указание провайдера
client = AIClientFactory.create_client(client_type="deepseek")
client = AIClientFactory.create_client(client_type="openai")
client = AIClientFactory.create_client(client_type="anthropic")

# Локальная модель (пока не реализована)
client = AIClientFactory.create_client(client_type="local", model_path="/path/to/model")
```

### Использование клиента

```python
# Обработка текстового промпта
result = client.process_prompt(
    prompt="Верифицируй энергетические данные...",
    system_message="Ты эксперт по верификации данных",
    max_tokens=2000,
    temperature=0.2
)

print(result["text"])
print(result["usage"])
print(result["model"])

# Обработка промпта с изображениями
result = client.process_vision_prompt(
    images=[base64_image1, base64_image2],
    prompt="Распознай текст на изображении",
    max_tokens=4000
)

# Получение метрик
metrics = client.get_usage_metrics()
print(f"Всего запросов: {metrics['total_requests']}")
print(f"Всего токенов: {metrics['total_tokens']}")
print(f"Среднее время ответа: {metrics['average_response_time']:.2f}s")
```

## Конфигурация

### Переменные окружения

```env
# Предпочтительный провайдер
AI_PREFERRED_PROVIDER=auto  # auto, local, openai, anthropic, deepseek

# Автоматический fallback на API
AI_FALLBACK_TO_API=true

# API ключи
OPENAI_API_KEY=sk-...
ANTHROPIC_API_KEY=sk-ant-...
DEEPSEEK_API_KEY=sk-...

# Пути к локальным моделям (для будущего)
LOCAL_AI_MODEL_PATH=/models/energy_verifier
LOCAL_MODEL_OCR_ENHANCER=/models/ocr_enhancer
LOCAL_MODEL_ENERGY_VERIFIER=/models/energy_verifier

# Настройки по умолчанию
AI_MAX_TOKENS=2000
AI_TEMPERATURE=0.2
AI_TIMEOUT=60
```

### Использование конфигурации

```python
from ai_config import get_ai_config, get_preferred_provider, is_ai_enabled

config = get_ai_config()
provider = get_preferred_provider()

if is_ai_enabled():
    client = AIClientFactory.create_client(client_type=provider)
```

## Преимущества архитектуры

### 1. Обратная совместимость
- Существующий код продолжает работать без изменений
- Текущая реализация через `ai_parser.py` не затронута

### 2. Плавный переход
- Можно тестировать локальные модели параллельно с API
- Переключение через конфигурацию без изменения кода

### 3. Гибкая конфигурация
- Выбор провайдера через переменные окружения
- Автоматический fallback на API при недоступности локальной модели

### 4. Унифицированный интерфейс
- Все модули работают с `BaseAIClient`
- Легко добавить новые провайдеры

## План миграции на локальные модели

### Этап 1: Текущее состояние (выполнено)
- ✅ Создана базовая архитектура
- ✅ Реализованы API-клиенты
- ✅ Создана заглушка для локальных моделей
- ✅ Создана фабрика клиентов

### Этап 2: Будущая реализация (TODO)
- 🔄 Реализовать `LocalAIClient.load_model()`
- 🔄 Добавить загрузку моделей в Dockerfile
- 🔄 Настроить конфигурацию для локального использования
- 🔄 Протестировать производительность

### Этап 3: Интеграция (TODO)
- 🔄 Обновить существующие AI-модули для использования фабрики
- 🔄 Добавить поддержку локальных моделей в модули
- 🔄 Настроить автоматический выбор провайдера

## Пример будущей реализации LocalAIClient

```python
# В будущем, когда будет реализовано:
def load_model(self) -> bool:
    from transformers import AutoModelForCausalLM, AutoTokenizer
    import torch
    
    self.tokenizer = AutoTokenizer.from_pretrained(self.model_path)
    self.model = AutoModelForCausalLM.from_pretrained(
        self.model_path,
        torch_dtype=torch.float16,
        device_map="auto"
    )
    self.is_loaded = True
    return True

def process_prompt(self, prompt: str, ...):
    inputs = self.tokenizer(prompt, return_tensors="pt")
    outputs = self.model.generate(
        inputs.input_ids,
        max_length=max_tokens,
        temperature=temperature
    )
    return self.tokenizer.decode(outputs[0], skip_special_tokens=True)
```

## Мониторинг и метрики

Все клиенты предоставляют метрики использования:

```python
metrics = client.get_usage_metrics()
# {
#     "total_requests": 100,
#     "total_tokens": 50000,
#     "total_cost": 1.50,  # Для API провайдеров
#     "average_response_time": 2.5,
#     "errors_count": 0,
#     "type": "api",  # или "local"
#     "provider": "deepseek"
# }
```

## Обработка ошибок

Система gracefully обрабатывает ошибки:
- Если локальная модель недоступна → автоматический fallback на API
- Если API недоступен → возвращается None, модули используют fallback
- Все ошибки логируются для мониторинга

## Следующие шаги

1. **Обновить существующие AI-модули** для использования новой архитектуры
2. **Добавить тесты** для проверки работы фабрики и клиентов
3. **Реализовать LocalAIClient** когда будут готовы локальные модели
4. **Добавить мониторинг** использования AI-клиентов

## Важные замечания

- **Текущая функциональность не изменена** - существующий код продолжает работать
- **Локальные модели пока не реализованы** - это заглушка для будущего
- **Автоматический fallback** - система всегда пытается использовать доступный провайдер
- **Единый интерфейс** - все клиенты работают одинаково через `BaseAIClient`

