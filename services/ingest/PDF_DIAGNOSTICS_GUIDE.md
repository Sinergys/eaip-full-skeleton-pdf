# 🔍 Руководство по диагностике PDF файлов

## Обзор

Это руководство описывает инструменты и методы диагностики проблем с распознаванием отсканированных PDF файлов в системе EAIP.

## 📋 Содержание

1. [Быстрая диагностика](#быстрая-диагностика)
2. [Детальный анализ](#детальный-анализ)
3. [Тестовые сценарии](#тестовые-сценарии)
4. [Решение проблем](#решение-проблем)

---

## 🚀 Быстрая диагностика

### Через командную строку

```bash
# Перейти в директорию сервиса
cd eaip_full_skeleton/services/ingest

# Запустить диагностику для конкретного PDF
python utils/pdf_diagnostics.py "путь/к/файлу.pdf"
```

### Через API

```bash
# Диагностика по пути к файлу
curl "http://localhost:8001/api/diagnose/pdf?file_path=/data/inbox/test.pdf"

# Диагностика по batch_id загруженного файла
curl "http://localhost:8001/api/diagnose/pdf?batch_id=xxx-xxx-xxx"
```

### Через Python код

```python
from utils.pdf_diagnostics import diagnose_pdf, print_diagnostic_report

# Диагностика
report = diagnose_pdf("путь/к/файлу.pdf")

# Вывод отчета
print_diagnostic_report(report)
```

---

## 🔬 Детальный анализ

### 1. Анализ структуры PDF

Диагностический инструмент проверяет:

- **Метаданные PDF**: количество страниц, информация о документе
- **Текстовый слой**: количество символов, слов, страниц с текстом
- **Изображения**: наличие изображений на страницах
- **Тип PDF**: текстовый или сканированный

### 2. Определение сканированного PDF

Система использует несколько критериев:

1. **Среднее количество символов на страницу**
   - < 10 символов → определенно сканированный (высокая уверенность)
   - < 50 символов → вероятно сканированный (средняя уверенность)
   - ≥ 50 символов → текстовый PDF

2. **Покрытие текстом**
   - < 10% страниц с текстом → определенно сканированный
   - < 30% страниц с текстом → вероятно сканированный
   - ≥ 30% страниц с текстом → текстовый PDF

3. **Комбинированный критерий**
   - PDF считается сканированным если: `avg_chars < 50` ИЛИ `text_coverage < 30%`

### 3. Тестирование OCR конвейера

Проверяется:

- **Конвертация PDF в изображения**: доступность poppler
- **OCR распознавание**: доступность Tesseract
- **Извлечение текста**: количество символов, извлеченных из первой страницы

---

## 🧪 Тестовые сценарии

### Просмотр доступных сценариев

```bash
python utils/test_pdf_scenarios.py
```

### Типы тестовых PDF

1. **Чисто текстовый PDF**
   - Ожидается: `is_scanned = False`, OCR не запускается
   - Создание: экспорт из Word/текстового редактора

2. **Отсканированный PDF**
   - Ожидается: `is_scanned = True`, OCR запускается
   - Создание: сканирование документа и сохранение как PDF

3. **Гибридный PDF**
   - Ожидается: обработка текста + возможный OCR для изображений
   - Создание: PDF с текстом и вставленными изображениями

4. **PDF с минимальным текстом**
   - Ожидается: `is_scanned = True`, попытка OCR
   - Создание: PDF только с изображениями

---

## 🛠️ Решение проблем

### Проблема 1: PDF не определяется как сканированный

**Симптомы:**
- PDF содержит только изображения, но система не запускает OCR
- `is_scanned = False` для явно сканированного документа

**Диагностика:**
```bash
python utils/pdf_diagnostics.py "проблемный_файл.pdf"
```

Проверьте:
- `avg_chars_per_page` - должно быть < 50
- `text_coverage` - должно быть < 30%
- `scanned_confidence` - должно быть "medium" или "high"

**Решение:**
- Если пороги не достигаются, возможно PDF содержит скрытый текстовый слой
- Попробуйте увеличить пороги в `file_parser.py` (строки 242-243)

### Проблема 2: OCR не запускается

**Симптомы:**
- `ocr_attempted = False` для сканированного PDF
- Сообщение "OCR библиотеки не установлены"

**Диагностика:**
```bash
# Проверка доступности библиотек
python -c "from pdf2image import convert_from_path; print('pdf2image OK')"
python -c "import pytesseract; print('pytesseract OK')"
```

**Решение:**
- Установите недостающие библиотеки: `pip install pdf2image pytesseract`
- Установите poppler (см. `INSTALL_POPPLER.md`)
- Установите Tesseract OCR

### Проблема 3: OCR не извлекает текст

**Симптомы:**
- `ocr_attempted = True`, но `ocr_success = False`
- `ocr_error = "poppler_not_installed"`

**Диагностика:**
```bash
# Проверка poppler
pdftoppm -h  # Должно вывести справку

# Тест конвертации
python -c "from pdf2image import convert_from_path; images = convert_from_path('test.pdf', first_page=1, last_page=1); print(f'Извлечено {len(images)} изображений')"
```

**Решение:**
- Установите poppler (см. `INSTALL_POPPLER.md`)
- Проверьте PATH - poppler должен быть доступен из командной строки

### Проблема 4: Низкое качество OCR

**Симптомы:**
- OCR запускается, но извлекает мало текста или с ошибками
- `total_chars_extracted` очень мало

**Диагностика:**
- Проверьте качество исходных изображений в PDF
- Проверьте DPI сканирования (рекомендуется ≥ 300 DPI)

**Решение:**
- Улучшите предобработку изображений (см. `preprocess_image_for_ocr`)
- Увеличьте DPI при конвертации (в `apply_ocr_to_pdf` измените `dpi=300` на `dpi=400`)

---

## 📊 Интерпретация результатов диагностики

### Ключевые поля отчета

```json
{
  "structure_analysis": {
    "scanned_detection": {
      "avg_chars_per_page": 2.5,        // Среднее символов на страницу
      "is_likely_scanned": true,        // Вероятно сканированный
      "is_definitely_scanned": true,    // Определенно сканированный
      "confidence": "high",              // Уверенность: high/medium/low
      "text_coverage": 5.0               // % страниц с текстом
    }
  },
  "ocr_test": {
    "can_convert": true,                // Может конвертировать PDF
    "can_ocr": true,                     // Может выполнить OCR
    "total_chars_extracted": 1234        // Символов извлечено
  },
  "summary": {
    "pdf_type": "scanned",               // Тип: scanned/text
    "needs_ocr": true,                   // Требуется OCR
    "ready_for_processing": true         // Готов к обработке
  }
}
```

### Рекомендации

Система автоматически генерирует рекомендации на основе анализа:

- **"Установите pdfplumber"** - для лучшего анализа PDF
- **"Установите pdf2image"** - для конвертации PDF в изображения
- **"Установите poppler"** - для работы с pdf2image
- **"Установите pytesseract"** - для OCR распознавания
- **"PDF содержит достаточно текста"** - OCR не требуется

---

## 🔧 Расширенная диагностика

### Логирование детальной информации

Включите DEBUG логирование:

```python
import logging
logging.basicConfig(level=logging.DEBUG)
```

### Ручная проверка этапов

```python
from file_parser import parse_pdf_file, apply_ocr_to_pdf

# 1. Парсинг PDF
result = parse_pdf_file("test.pdf")
print(f"Тип: {'сканированный' if result['is_scanned'] else 'текстовый'}")
print(f"Символов: {result['total_characters']}")

# 2. Тест OCR (если нужно)
if result['is_scanned']:
    ocr_text = apply_ocr_to_pdf("test.pdf")
    print(f"OCR извлек: {len(ocr_text)} символов")
```

---

## 📝 Чек-лист диагностики

- [ ] PDF файл существует и доступен для чтения
- [ ] Установлены все необходимые библиотеки (pdfplumber, pdf2image, pytesseract)
- [ ] Poppler установлен и доступен в PATH
- [ ] Tesseract OCR установлен
- [ ] PDF правильно определяется как сканированный/текстовый
- [ ] OCR запускается для сканированных PDF
- [ ] OCR успешно извлекает текст
- [ ] Результаты сохраняются в базу данных

---

## 🆘 Получение помощи

Если проблема не решена:

1. Запустите полную диагностику: `python utils/pdf_diagnostics.py <файл>`
2. Проверьте логи сервера на наличие ошибок
3. Сохраните диагностический отчет для анализа
4. Проверьте соответствие PDF тестовым сценариям

---

**Последнее обновление:** 2025-01-13

